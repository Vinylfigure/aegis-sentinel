import { StubPage } from "@/components/StubPage/StubPage";

export default function HomePage() {
  return (
    <StubPage
      title="Aegis · demo engagement"
      plannedIn="A1"
      description="Frontend scaffold for the Aegis assurance compiler. The tabs above are the planned route arc: scope and controls design the program (A3–A4), process shows controls on the flow (A5), reconciliation through proof render the engagement artifacts (B2–B4, wired to real artifacts/demo-engagement output in C2)."
    />
  );
}
