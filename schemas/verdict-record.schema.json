{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Vinylfigure/aegis-sentinel/schemas/verdict-record.schema.json",
  "title": "Aegis Sentinel verdict record",
  "description": "One population-scoped control verdict with its full audit trail. Conventions forked from GRCEngClub finding.schema.json v1.0.0 (knowledge/01_corpus/05_prior_art/Aegis_Prior_Art_GRCEngClub_Toolkit.md §3.1): const-pinned schema_version, closed objects, conditional requirements, the source/source_version/run_id/collected_at reproducibility tuple, severity independent of status. Deltas per docs/EVALUATION.md §2: record_hash + chain_prev, population_id/population_count/completeness_ref, spec_id + spec_hash, test_function_version, ratification_ref, and unknown_cause as the required companion of UNKNOWN. skipped is deliberately absent: a population-testing system has no skip state that is not either NOT_APPLICABLE or a completeness failure.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "record_id",
    "control_id",
    "status",
    "source",
    "source_version",
    "run_id",
    "collected_at",
    "record_hash",
    "chain_prev",
    "population_id",
    "population_count",
    "completeness_ref",
    "spec_id",
    "spec_hash",
    "test_function_version",
    "evidence_refs"
  ],
  "properties": {
    "schema_version": {
      "const": "0.1.0",
      "description": "const-pinned semver; consumers pin a major version"
    },
    "record_id": { "type": "string", "minLength": 1 },
    "control_id": {
      "type": "string",
      "minLength": 1,
      "description": "Control identifier from the testing library, e.g. CM-03"
    },
    "status": { "enum": ["PASS", "FAIL", "UNKNOWN", "NOT_APPLICABLE"] },
    "unknown_cause": {
      "enum": ["basis_missing", "identity_fuzzy", "no_basis_anywhere"],
      "description": "Required companion of status=UNKNOWN (D-7): which family of missing basis"
    },
    "message": { "type": "string", "minLength": 1 },
    "severity": {
      "enum": ["critical", "high", "medium", "low", "informational"],
      "description": "Independent of status: a passing critical control still tells the consumer the stakes"
    },
    "support": {
      "type": "object",
      "additionalProperties": false,
      "required": ["field_values", "record_hashes"],
      "description": "FAIL and UNKNOWN verdicts must carry support; PASS may be terse",
      "properties": {
        "field_values": { "type": "object" },
        "record_hashes": {
          "type": "array",
          "items": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
        }
      }
    },
    "source": { "type": "string", "minLength": 1 },
    "source_version": { "type": "string", "minLength": 1 },
    "run_id": { "type": "string", "minLength": 1 },
    "collected_at": { "type": "string", "format": "date-time" },
    "record_hash": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "SHA-256 of the evidence record at intake (D-1)"
    },
    "chain_prev": {
      "type": ["string", "null"],
      "pattern": "^[0-9a-f]{64}$",
      "description": "Hash-chain link to the previous record; null only for the first record in a chain"
    },
    "population_id": { "type": "string", "minLength": 1 },
    "population_count": { "type": "integer", "minimum": 0 },
    "completeness_ref": {
      "type": "string",
      "minLength": 1,
      "description": "Pointer to the completeness assertion for the population this verdict is scoped to"
    },
    "spec_id": { "type": "string", "minLength": 1 },
    "spec_hash": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "Hash of the frozen collection spec that produced the record: run_id says when, spec_hash says under what ratified plan"
    },
    "test_function_version": {
      "type": "string",
      "minLength": 1,
      "description": "Version of the pure function that emitted the verdict, with its seeded-fixture reference"
    },
    "ratification_ref": {
      "type": "string",
      "minLength": 1,
      "description": "Pointer to the hashed human sign-off; required when status=NOT_APPLICABLE"
    },
    "evidence_refs": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Typed pointers to backing artifacts — pointers, never payloads"
    }
  },
  "allOf": [
    {
      "if": { "properties": { "status": { "const": "FAIL" } }, "required": ["status"] },
      "then": { "required": ["message", "severity", "support"] }
    },
    {
      "if": { "properties": { "status": { "const": "UNKNOWN" } }, "required": ["status"] },
      "then": { "required": ["message", "unknown_cause", "support"] }
    },
    {
      "if": { "properties": { "status": { "const": "NOT_APPLICABLE" } }, "required": ["status"] },
      "then": { "required": ["ratification_ref"] }
    }
  ]
}
