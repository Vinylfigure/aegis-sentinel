{
  "rate_limits": null,
  "history_caveats": [
    "DRAFT: exact endpoint path (assumed GET /api/hris/v1/employees, Merge's standard category-prefixed Unified API convention) and any filtering options beyond employment_status were not independently re-fetched this session -- docs.merge.dev's API-reference page renders via JS and returned no endpoint-path content through this session's proxy; verify against a reachable rendering before ratification",
    "Merge is an aggregator over the tenant's actual HRIS system -- employment_status/termination_date freshness depends on Merge's own sync cadence with the underlying HRIS, not a property of this capability entry"
  ],
  "ratified_by": null,
  "lifecycle": "draft",
  "id": "merge.hris.employees",
  "system": "merge",
  "surface": "Merge Unified API GET /api/hris/v1/employees",
  "access_modes": [
    "direct-api"
  ],
  "populations_yielded": [
    {
      "type": "entity",
      "description": "employee directory records with employment status and termination date",
      "attributes": [
        "id",
        "employment_status",
        "termination_date",
        "start_date"
      ]
    }
  ],
  "temporal": {
    "kind": "state-only"
  },
  "join_keys": [
    "employee_id",
    "email"
  ],
  "auth_scope": "API key (Authorization: Bearer <token>) plus X-Account-Token identifying the linked HRIS tenant -- confirmed via docs.merge.dev; Merge does not document a separate per-category permission scope beyond the account-level API key",
  "pagination": {
    "method": "cursor",
    "exhaustion_method": "pass the `next` field as the `cursor` query param until it is null (page_size default 30, max 100)"
  },
  "provenance": {
    "researched_by": "vinylfigure",
    "researched_at": "2026-09-05T00:00:00Z",
    "citations": [
      "https://docs.merge.dev/hris/employees/",
      "https://docs.merge.dev/basics/pagination/",
      "https://docs.merge.dev/hris/"
    ],
    "doc_version": null
  }
}
