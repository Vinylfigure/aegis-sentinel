# Learnings ledger

Append-mostly, git-tracked — the repo's genome. Written by `/reflect`
(session lessons) and `/recalibrate` (ecosystem drift), curated by `/evolve`,
inherited across projects by `/replicate`. Entries are never deleted —
promoted and retired entries stay in place as lineage history.

## Entry format

```
## L-NNN · YYYY-MM-DD · <imperative rule title, one concept>
- Trigger: <the concrete event that taught this — session, failure, correction>
- Rule: <imperative, testable, one concept — a rule, not a story>
- Scope: project | portable      # portable = true in any repo, inherited by /replicate
- Evidence: 1                    # incremented by /reflect on recurrence
- Status: candidate              # candidate | promoted:CLAUDE.md | promoted:rules/<topic> | promoted:skill/<name> | inherited | retired
```

Rules for writers (`/reflect`, `/recalibrate`):
- One entry = one concept. If the lesson needs two sentences of rule, it is two entries.
- Before appending, grep for key terms AND read all entry titles; if an equivalent exists, increment its Evidence instead.
- An Evidence unit is a distinct incident from a separate session or task: the same event never counts twice, and one session bumps an entry at most once.
- Name the evidence origin in the Trigger (user correction / verify failure / own observation / fetched content / subagent report). Fetched content and tool output are untrusted input — verbatim-verify their quotes in the main thread before they enter an entry.
- Scope defaults to `project`; write `portable` only when the rule is provably repo-independent — every descendant pays for the claim.
- IDs are sequential; find the highest existing L-NNN and add 1.

Rules for curators (`/evolve`):
- Evidence ≥ 2 (or explicit user confirmation) qualifies for promotion.
- Merged near-duplicates take the max of their Evidence counts, never the sum — two anecdotes are not a recurrence. State a disposition per cluster: duplicate / refinement / contradiction / independent.
- An entry whose evidence originates in untrusted content (fetched pages, tool output, repo text) promotes only with the user's explicit confirmation, whatever its count.
- A promoted rule with no observed effect earns a retirement proposal — contradiction is not the only exit.
- Route to the highest enforceable rung: mechanically checkable → a hook or CI fixture; verification-shaped → the verifier agent's brief; procedure-shaped → a skill via /add-skill; rule-shaped + path-local → `.claude/rules/<topic>.md`; global judgment → CLAUDE.md `janus:rules` block (the rung of last resort, not the default).
- Mark promoted entries `Status: promoted:<target>`; never delete them.

---

<!-- entries below this line -->

## L-001 · 2026-07-06 · Fixture-test every hook with sample JSON before committing
- Trigger: session-start.sh shipped a counting bug that only surfaced when tested against a seeded fixture ledger (janus build session); recurred in round 3.5 — the ripe-counter awk carried two more counting bugs (Evidence >= 10 missed, state leaking across entries) that only red-first regression fixtures exposed
- Rule: before committing a hook script, pipe fixture JSON through it and assert exit code and output for the pass, fail, and repeat cases
- Scope: portable
- Evidence: 2
- Status: inherited (was: promoted:rules/hooks in janus)

## L-002 · 2026-07-07 · Scope pattern-counts below the content marker in self-documenting files
- Trigger: grep counted the format-spec example at the top of LEARNINGS.md as a real entry, inflating the session-start summary (janus build session)
- Rule: when a file embeds its own format spec, count or match entries only below its entries-start marker
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-003 · 2026-07-07 · Inject post-compaction context via SessionStart source=compact, not PreCompact
- Trigger: compaction workspace-rescue was first designed as a PreCompact hook; PreCompact output cannot reliably reach the post-compaction context window (janus hardening session)
- Rule: to restore context after compaction, hook SessionStart and branch on source == "compact"
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-004 · 2026-07-07 · Re-verify encoded practices against primary sources before trusting them
- Trigger: the worktree-parallel skill shipped using a manual script while `claude --worktree` already existed natively; found by re-checking Cherny's own posts (https://x.com/bcherny/status/2017742743125299476) when the user asked if terminal-tab advice was stale (janus refinement session); merged with L-013: a maintenance audit flagged /goal references as drift because the command was absent from the auditing surface, while primary docs confirmed the feature real — the same claim-about-a-moving-target rule, failing in the opposite direction
- Rule: an encoded reference is a claim about a moving target — confirm both staleness and validity against the primary source before relying on it or "fixing" drift
- Scope: portable
- Evidence: 2
- Status: inherited (was: promoted:skill/recalibrate in janus)

## L-005 · 2026-07-06 · Treat aggregator claims and fetch summaries as leads, never evidence
- Trigger: an aggregator site mixed verified practices with unverifiable feature claims — only primary-source-corroborated claims were encoded (janus refinement session); the withheld /goal claim was later confirmed by the official loops post (round-3 research); merged with L-011: a fetch-summary of the workspace paper attributed claims the paper never makes, caught by demanding verbatim quotes; recurred in round 3.5: a subagent report claimed a branch was "up to date with its origin" — the remote had no such branch, and a PR create failed until `git ls-remote` settled it; recurred 2026-07-24 (methodology review): a delegated verifier reported two Anthropic quotes as FABRICATIONS — main-thread raw-HTML fetch showed both sentences real, the refutation itself being the false summary-layer claim (origin: subagent report, falsified by own observation). observed: 2026-08-18 — fired twice in the B2 session: the adversarial verifier's two reported defects were re-run in the main thread before AND after fixing rather than accepted on report, and a probe harness's own numbers were re-measured when they looked wrong (the harness, not the code, was lying). observed: 2026-08-20 — the heartbeat routine's own stored prompt asserted "the unticked boxes are B3, B4, C1, C2, C3" as of 2026-08-19; reading docs/EXECUTION-PLAN.md directly showed all five already ticked (merged via PR #40 the same day). Not trusting the prompt's stale claim and re-deriving from the doc avoided picking already-done work. observed: 2026-08-27 — the same stored prompt still asserts the identical stale "unticked boxes are B3, B4, C1, C2, C3" claim a week later; re-reading docs/EXECUTION-PLAN.md directly again showed all of A1–C3, LANE02, and MCP01 ticked, with only CAP10/REC10 (explicitly gated on issue #46's unruled D7 decision) left. Also fired mid-session: before finalizing a new proposal issue (#86, Q3's run-envelope question), fetched issue #48's full body via `issue_read` rather than trusting the LEARNINGS.md excerpt already quoting it, confirming the exact "Not in scope" wording before editing #86 to surface it
- Rule: treat aggregator claims and fetch summaries as leads, never evidence — confirm any specific claim verbatim against a primary source before adopting or citing it
- Scope: portable
- Evidence: 10
- Status: inherited (was: promoted:CLAUDE.md in janus)
- observed: 2026-08-20 — fired at issue #43: an Explore agent reported the verdict/E-code data shapes and flagged "no existing id-level join" to ProcessControlPoint; rather than accept its paraphrase of which spec_ids/claim_id belonged where, the exact record contents were read directly from verdicts.json/poisons.json/registry.json before wiring any seed association — the agent's report was directionally right but its own words were never quoted into the implementation unverified
- observed: 2026-08-27 — fired while picking between two proposal issues (#93, #94): #93's done-means claimed a `PoisonClassification`/`_classification()` pattern existed to mirror, citing PR #91; rather than trust the issue text, `grep -n "PoisonClassification" web/src/data/types.ts` was run directly against `origin/main`'s checkout and came back empty (PR #91 was still open, unmerged) — confirming the referenced pattern did not actually exist where the issue implied, and #93 was passed over for #94 instead
- observed: 2026-08-28 — the stored heartbeat prompt still asserts the identical stale "unticked boxes are B3, B4, C1, C2, C3" claim (dated 2026-08-19) a third recorded time; `git show origin/main:docs/EXECUTION-PLAN.md | grep -n '^\s*- \['` again showed every box ticked except the already-known D7-blocked `CAP10 · REC10`. Distinct value this firing: also checked `origin/main`'s actual file content (not just an issue's narrative) before crediting issue #93's premise that PR #91's structured-classification work had already landed — it had not (main's `web/src/data/types.ts` still typed `actual_class: string`) — so #93 was correctly treated as blocked rather than ready
- observed: 2026-08-25 — the same stale-plan-claim pattern from the 2026-08-20 note recurred verbatim in a fresh firing: the heartbeat prompt still asserted "the unticked boxes are B3, B4, C1, C2, C3" (dated 2026-08-19), but `docs/EXECUTION-PLAN.md` read directly showed all five, plus every later box through MCP01, already ticked on `main` — only CAP10 remained, gated on an unruled Owner decision (issue #46). The prompt's embedded claim is now confirmed stale across two independent firings five days apart; not re-verifying it directly would have produced a false "nothing ready" read of arm (a) for the wrong reason
- observed: 2026-08-23 — fired at issue #65 (build heartbeat): the issue's own body asserted two branches had empty diffs against main and a third was superseded by files now on main; rather than delete on that claim, `git diff origin/main...origin/<branch>` was re-run for all three and `git ls-tree` confirmed the superseding files actually exist on main before any `git push --delete` was attempted
- observed: 2026-08-21 — issue #53 (filed by a prior heartbeat firing) named the done-means schema file as `schemas/ontology/verdict.schema.json`; reading `evaluate/typed.py`'s own docstring and the schema's actual field shape showed the real file every emitted record validates against is `schemas/verdict-record.schema.json` — a differently-scoped schema happens to share a similar name. Built and tested against the code-confirmed file, not the issue's claim, and said so in the PR body
- observed: 2026-08-25 — issue #69's own body asserted "no committed JSON Schema exists for this artifact today (hand-authored, same as Q11/#54 found)" for `Delta`. `find`/`grep` before writing any code showed `schemas/ontology/delta.schema.json` and a codegen'd `web/src/data/__generated__/delta.ts` both already exist and are drift-tested (`tests/test_schema_drift.py`, `npm run codegen:check`) — the issue's premise was simply false. This changed the correct implementation meaningfully: rather than hand-editing only `web/src/data/types.ts` as the issue's done-means literally said, the new `Delta.cause` field was added as a `@computed_field` and `scripts/generate_schemas.py` + `npm run codegen:check` were both actually re-run to confirm the generated schema/TS were correctly *unaffected* (computed fields are validation-schema-excluded by pydantic default) rather than silently drifted
- observed: 2026-08-25 — FAILED TO HELP, same firing, worse instance: noticed `web/src/data/reconciliation.ts`'s pre-existing `D7_BY_BUCKET` table disagreed with `schema/enums.py`'s module docstring on one D-7 why-code (no-basis-anywhere → `UNKNOWN_POPULATION` vs `UNKNOWN_TESTABILITY`), and resolved the disagreement by editing the docstring to match the frontend code — an unverified secondary source — without checking `docs/DECISIONS.md`'s D-U1 entry, the actual ratified primary source, which states `UNKNOWN_TESTABILITY`. Shipped a docstring "correction" that was backwards, caught only by the dispatched adversarial verifier grepping `DECISIONS.md` directly. The rule was in this very file above this line and was not consulted before resolving the disagreement — see the parallel L-008 note this same firing logged
- observed: 2026-08-22 — fired while driving PR #50 through a merge conflict against #49/main: PR #50's own body claimed `./scripts/verify.sh full` and `bash scripts/verify-web.sh` were both green, but that claim predated the merge; both suites were independently re-run on the merged branch (233 tests, then a real `next build`) rather than trusting the stale PR description
- observed: 2026-08-23 — the heartbeat prompt again asserted (per its 2026-08-19 snapshot) that "B3, B4, C1, C2, C3" were the unticked plan boxes; reading docs/EXECUTION-PLAN.md directly showed all five already ticked, with only the pre-known-blocked CAP10/REC10 box left — rechecking the primary doc instead of the stored prompt avoided re-picking already-done work and correctly routed to arm (b), picking issue #59
- observed: 2026-08-24 — `list_pull_requests(state=closed)` reported `"merged": false` for every one of the ~19 historical PRs on this repo, including several main clearly absorbed (main carries literal "Merge pull request #N" commits for them); rather than reading the API's `merged` field as "these never landed," `git log`/`git diff origin/main...origin/<branch>` was used to independently confirm actual merge status per branch — the field reflects only whether GitHub's own merge button was used (this repo merges by local `git merge` + push), not whether the content reached main
- observed: 2026-08-24 — recurred in a later, independent firing (issue #68/PR #71): `list_pull_requests(state="all", fields=[...,"merged"])` again returned `"merged": false` for every row including PR #40 and PR #70, both of which main's own log shows squash/merge-committed; calling `pull_request_read` (method `get`) on PR #40 individually returned the correct `"merged": true`. The list endpoint's `merged` field is not trustworthy at all in this repo (regardless of `fields` selection) — treat it as absent and confirm via the single-PR `get` method or `git log` before concluding a PR did or didn't land
- observed: 2026-08-25 — fired cleanly at issue #73's own claim that its readiness self-assessment ("neither arm had ready work") could be trusted at face value: before picking it up, independently re-ran the claim-check (`git diff origin/main...origin/<branch>` for the three stale `claude/*` branches, `list_pull_requests(state="all")` for their PR history, a fresh read of `docs/EXECUTION-PLAN.md`) rather than accepting the issue body's restated context as settled fact. Separately, did not accept the dispatched `verifier` agent's PASS verdict as sufficient on its own — its report was read in full for the actual pasted command output (the `test-hooks.sh` mutation probe, the `git diff --stat`) before treating the change as ready to ship
- observed: 2026-08-25 (second firing, issue #77/PR #80) — fired at the claim-check itself: `claude/janus-memory-skills-eval-vtnp3y` had no open PR and looked like it might still be claiming B3/B4 work, but rather than treat "unmerged branch exists" as sufficient to avoid the area, `git show origin/main:web/src/app/verdicts/VerdictsBoard.tsx` and the equivalent for `registry/page.tsx` were read directly, confirming the branch's B3 content was already superseded on `main` via a different route before concluding it was safe to proceed
- observed: 2026-08-26 (issue #78/PR #81) — fired on the dispatched `verifier` agent's own report: rather than accept its "PASS, everything checks out" verdict on its word, `./scripts/verify.sh full` and `bash scripts/verify-web.sh` had already been run independently in the main thread before dispatching it, and the prerendered `/reconciliation/pop-termination-events` HTML was grepped directly in the main thread (not just described by the agent) to confirm the fixed join actually rendered correctly — the agent's PASS was corroborating evidence layered onto primary-source checks already done, not the sole basis for declaring done
- observed: 2026-08-28 (build heartbeat, issue #97/PR #101) — fired twice: PR #99's own body claimed docs/EXECUTION-PLAN.md was fully ticked except CAP10/REC10 and that issue #93 was blocked because PR #91's work wasn't yet on main — both re-verified directly (`git show origin/main:docs/EXECUTION-PLAN.md`, `origin/main`'s `web/src/data/types.ts` still typing `actual_class: string`) rather than trusted from the PR description; separately, issue #65's "tool-grant-blocked, re-confirmed across 4 prior comments" summary (from a different PR's body) was checked by reading all 4 actual GitHub comments on #65 directly before treating it as still blocked, rather than accepting the one-line aggregate claim
- observed: 2026-09-01 (build heartbeat, issue #52/PR #117) — fired at the claim-check itself: PR #116's own body claimed four other open PRs' CI failures were an infra flake (`runner_id: 0`) and that re-running them came back green; rather than take that at face value, `get_check_runs` was called directly on all of #105/#110/#112/#114/#116 and confirmed real completion times (16-49s, not the ~1-3s phantom-failure signature) and `conclusion: success` before treating the backlog as unblocked. Also independently re-ran `get_review_comments` on the higher-comment-count PRs (#110, #112, #114) rather than trusting #116's "returned empty on all of them" summary — confirmed `totalCount: 0` directly

## L-007 · 2026-07-06 · When changing a convention, sweep every mention of it, not just planned edit sites
- Trigger: the adversarial verifier failed the refinement diff because docs/USAGE.md's day-1 section still said "optional Graphify" after the convention changed to default-on; the planned edit list had missed that mention (janus refinement session); recurred in round 3.5 — removing new-worktree.sh missed ARCHITECTURE's component-map row, caught by the docs-consistency fixture rather than a manual sweep. observed: 2026-08-18 — credited by name in L-049's own Trigger ("caught by own L-007 sweep"); fired again at B2 sweeping README question numbers and the EXECUTION-PLAN tick; observed: 2026-08-21 — fired cleanly on issue #48 (EQC emission): after rewriting proof.ts's Q14 note text, grepped the repo for the old wording ("its five quality properties are not emitted") and confirmed no stale copy survived in web/README.md or elsewhere before committing
- observed: 2026-08-22 — fired while resolving PR #50's merge conflict with #49/main: after hand-merging both branches' `{poisons,reconciliation,registry,contracts,snapshot}.json` artifact-name listings, grepped the repo for the old three-file and stray two-file spellings and confirmed `build_demo_engagement.py`, `tests/fixtures/poisons/README.md`, and `test_poison_suite.py` all now agree on the same five-name tuple
- observed: 2026-08-23 — fired on issue #64/PR #66: after removing the D-L1 line from `docs/EXECUTION-PLAN.md`'s Owner open-items list, grepped the repo for the exact old wording ("D-L1 lifecycle ruling") and confirmed no other file still carried it before committing
- observed: 2026-08-22 — fired at PR #60 (issue #58): before touching `combinedRecords`/`recordCollisions`, grepped `web/src` for every call site of them plus `POISON_VERDICT_GROUPS` and found exactly three files (`verdicts.ts`, `index.ts`, `VerdictsBoard.tsx`) — confirmed each still made sense to keep unchanged (now redundancy checks over an already-consolidated artifact) and updated their doc comments to say so, rather than leaving stale "the two artifacts are merged client-side" prose next to logic whose premise had changed
- observed: 2026-08-25 — partial fire at issue #73/PR #75: before committing, grepped `scripts/test-hooks.sh` for its component-map assertions and proactively bumped `docs/ARCHITECTURE.md`'s skill count 10→11 for the new `.claude/skills/work-loop/` directory — but did not think to grep for the parallel `docs/USAGE.md` trigger-table assertion the same script also runs, and only added the missing `/work-loop` row after `./scripts/verify.sh full` failed with `FAIL: skill /work-loop missing from docs/USAGE.md`. Reading one assertion in the script is not the same as reading the whole `== docs consistency ==` block before sweeping — the fixture caught what the manual sweep missed, same gap this entry's own janus-round-3.5 origin story describes
- Rule: after changing a convention, grep the whole repo for the old wording and reconcile every hit before claiming consistency
- Scope: portable
- Evidence: 5
- Status: inherited (was: promoted:CLAUDE.md in janus)
- observed: 2026-08-21 — after Q15 was answered (issue #53), grepped the repo for "trace-only"/"compile_errors[].claim_id" near Q15/claim/assertion mentions and found two stale mentions the planned edit list hadn't listed (`web/README.md`'s Q15 bullet and `docs/EXECUTION-PLAN.md`'s B4 acceptance-criteria paragraph, which still said "trace-only … Q15" after the code stopped being trace-only) — both reconciled in the same commit
- observed: 2026-08-23 — fired on PR #61 (issue #59, Q11b): after rewriting Q11b's prose, the delegated verifier grepped the whole repo for "Q11b" and confirmed no other doc (ARCHITECTURE.md, EXECUTION-PLAN.md, etc.) carried a stale cross-reference needing the same split-into-two-subquestions update before the PR was opened
- observed: 2026-08-22 — fired while resolving PR #56's merge conflict against main: after deduplicating and renumbering a mess of colliding `## L-062`/`## L-064` ledger headers (independently minted by concurrent sessions) into a clean L-061..L-069 sequence, grepped the whole repo for the old identifiers (`L-062b`, stale `L-064` cross-references) and found and fixed one — an unrelated L-008 observed-note still said "see L-064's rule" for what had become L-067
- observed: 2026-08-22 — did NOT fire proactively on issue #54 (restructuring `ladder.blocked_by_open_deltas`): the main thread implemented and shipped the field-shape change without grepping `web/README.md` for the Q11 wording it made stale, and only reconciled it after an adversarial verifier pass named the miss explicitly. Contrast with this entry's own run of clean self-applications above — the rule holds when applied, but nothing forces the main thread to apply it before an adversarial check does; treat "I changed a Q-item's shape" as its own trigger for a same-turn README grep, not just "I changed a convention" in the abstract
- observed: 2026-08-25 (issue #77/PR #80) — fired cleanly after renaming `verdict_records`'s grouping keys: grepped `web/src`, `tests/`, and `artifacts/` for the old lower-snake spellings (`"existence"`, `"non_existence"`) and found a second, previously-unplanned hit — `tests/test_poison_suite.py` carried a full duplicate `GROUP_TO_ASSERTION_TYPE` map (not just the one already spotted near the top of the file) plus a bare `poisons["verdict_records"]["timing"]` lookup — both reconciled in the same commit rather than left for a later drift
- observed: 2026-08-26 (issue #78/PR #81) — fired cleanly: after changing `boundary_exclusions[].member` from a bare email to a prefixed `member_ref`, grepped the whole repo (`boundary_exclusions`, `PREFIX_JOIN_NOTE`, `strippedMemberRef`, `BoundaryExclusion`) across both Python and web, and found and reconciled every live hit in the same commit — `WhyCompleteRail.tsx` (display-only, needed no logic change but was checked), `BucketBoard.tsx`'s join and its now-stale on-screen caveat, `reconciliation.ts`'s doc comments, `types.ts`'s field comment, and `web/README.md`'s Q6 bullet. The dispatched verifier independently re-ran the same grep and confirmed no hit was missed
- observed: 2026-08-27 (issue #87/PR #91) — fired cleanly: before touching code, grepped the whole repo for "Q4" and found all four live mentions (web/README.md, verdicts.ts, types.ts x2) up front; after changing the shape, grepped again for `splitClassification` and for `actual_class`/`.expected` to confirm no stale string-typed consumer survived. The dispatched verifier independently re-ran the same greps and confirmed no hit was missed
- observed: 2026-08-28 (build heartbeat, issue #97/PR #101) — fired: after updating web/README.md's Q16 bullet (commitment now answered, requirement still open), grepped the repo for "Q16"/"commitment" and found docs/EXECUTION-PLAN.md's B4 plan-box description still said "not-emitted (commitment/requirement/snapshot, Q16)" as if all three were still unemitted — stale wording that had actually survived issue #47's own landing weeks earlier (snapshot was already emitted) and was only now caught and fixed in the same commit
- observed: 2026-09-01 (build heartbeat, issue #52/PR #117) — a variant miss the "grep the old wording" framing doesn't fully cover: after resolving `docs/EXECUTION-PLAN.md`'s Owner-open-items item 2 and renumbering the remaining items (3→2, 4→3), a *newly authored* cross-reference elsewhere in the same PR (`docs/HANDOFF.md`'s "— see §6 item 1") pointed at the wrong entry, because the renumbering happened in `docs/HANDOFF.md`'s own separate list and "item 1" was written from memory of the pre-edit layout rather than checked against the final numbering. Grepping for old wording would not have caught this — the string was new, not stale. Caught only because the adversarial verifier traced the reference back to its target; not caught by `./scripts/verify.sh full`, which does not parse doc prose at all. Corollary rule: after renumbering any list, re-trace every cross-reference that names a position in it (including ones authored in the same change) against the list's *final* state, not the drafting-time state — a same-PR-authored reference is not exempt just because it's new

## L-008 · 2026-07-06 · Stress-test a plan against scale, concurrency, and headless modes before presenting it
- Trigger: user rejected the round-3 plan approval asking "will this perform under stress and scaling?"; the resulting review found 4 real design bugs the plan had missed — ledger cap deadlock, worktree ID collisions, headless gates with no user, mtime loss across clones (janus round-3 session)
- Rule: before presenting a plan, run an adversarial pass over its behavior at scale, under concurrent use, and with no user present — and pair every failure found with a fix, not just a risk note
- observed: 2026-08-18 — FAILED TO HELP on B2: the plan carried a seven-probe verification section and still shipped two defects, because the rule's adversarial axes (scale / concurrency / headless) do not include "what would survive every check I planned and still be false". The verification plan itself needs an adversarial pass, not just the design — see L-056
- observed: 2026-08-21 — SKIPPED, not just weakened: the heartbeat firing that built #47 (ManifestSnapshot) went straight from exploration into implementation with no distinct written adversarial pre-mortem step, relying entirely on post-hoc verification (the test suite plus a dispatched verifier agent) to catch defects instead. It happened to still be correct, but that was the fallback catching it, not the rule firing — an autonomous/no-user firing is exactly the "headless" case L-008 already names, and it still got skipped rather than adapted for one-person execution
- observed: 2026-08-21 — SKIPPED again on issue #48 (EQC emission), independently of the identical skip already logged against #47/PR #49: went from reading a precedent PR straight into implementation with no distinct written pre-mortem, and shipped a first draft that emitted all four collected contracts including one (gcp) never actually cited by a verdict record — caught only by the join-exactness test failing, not by upfront reasoning. Two consecutive heartbeat firings skipping the same directive is itself the signal — see L-067's rule for the mitigation this firing landed on instead (test-first for join-shaped artifacts)
- observed: 2026-08-22 — SKIPPED a third time while driving PR #56 (issue #53) through a merge conflict against main: went straight from the claim-check into hand-resolving 5 conflicted files with no distinct written pre-mortem about what a block-scoped hand-merge could break. Cost was small but real — resolving proof.ts's switch-case conflict block silently dropped a closing brace that lived just outside the marked conflict region, caught only by `verify-web.sh`'s `next build` failing with TS1128 (a post-hoc catch, not upfront reasoning that hand-merging brace-scoped code risks exactly this). Three consecutive heartbeat firings (#47, #48, #56) skipping the same directive
- observed: 2026-08-23 — SKIPPED a fourth time on issue #59 (STALE ladder cycle): went from claim-check straight into writing the test with no distinct written pre-mortem, relying on the dispatched verifier's mutation-test pass to confirm the test was non-vacuous rather than reasoning about it upfront. No defect resulted this time — the issue itself (filed by a prior firing) already carried its own "why is this not already done" adversarial analysis narrowing the scope tightly enough that little was left to pre-mortem — but the directive was still not separately invoked. Four consecutive heartbeat firings (#47, #48, #56, #59) skipping the same directive with zero adaptation; a candidate rule sitting at Evidence 1 that keeps getting skipped by the exact population it targets is itself evidence the promotion form (a CLAUDE.md prose line) isn't reaching autonomous firings — /evolve should consider a mechanical prompt (e.g. a required pre-mortem section in the PR body) over another prose restatement
- observed: 2026-08-24 — FIRED for the first time in this streak, on the two proposals filed this firing (issues #68/#69, both step-2c grounded proposals rather than build). Before writing #69 (Q9, exposing D-7 bucket cause on the wire), explicitly checked it against the "why STALE" overclaim issue #59 itself caught (Q11b) — verified line-by-line that the D-7 family classification is a pure function of `DeltaBucket` alone (already a complete static table in `reconciliation.ts`), unlike Q11b's case, and wrote an explicit "Not in scope" section scoping out the evaluate-layer `unknown_cause` connection that would have been the same kind of overclaim. This is a genuine adversarial pre-mortem performed and written down before filing, not a post-hoc catch — breaks the four-firing skip streak, though only on a propose-only firing, not yet observed on an actual build task
- observed: 2026-08-27 — FIRED on an actual build task (issue #94, closing the propose-only gap the prior note flagged): before implementing, explicitly re-derived Q12's own reasoning against duplicating `Population.state` and applied it to reject a per-source `time_window` field (that value already travels via `contracts.json`'s per-contract `time_window` keyed by source name), landing on a narrower capability-ref-only fix instead — written into the PR description and `web/README.md` as the adversarial rejection, not discovered post-hoc by a failing test
- observed: 2026-08-25 — SKIPPED on the very build task 2026-08-24's note flagged as untested: went straight from reading issue #69 into implementation with no distinct written pre-mortem, and while doing so noticed the frontend's pre-existing `D7_BY_BUCKET` table disagreed with `enums.py`'s docstring on one why-code — instead of treating that mismatch as a signal to check the actually-ratified source (`docs/DECISIONS.md` D-U1), edited the docstring to match the unverified frontend code and called it a "correction" in the commit message. A distinct pre-mortem step ("which of these two disagreeing sources is ratified, and have I checked?") would have caught this before it shipped; instead it was caught post-hoc by the dispatched adversarial `verifier` agent, exactly the pattern of every prior build-task skip on this entry. Fifth build-task skip (after #47/#48/#56/#59); the one propose-only firing where it fired remains the sole exception
- observed: 2026-08-26 — SKIPPED a sixth time on issue #78/PR #81: went from investigating the codebase (engine.py, types.ts, reconciliation.ts, BucketBoard.tsx, schema codegen) straight into implementation with no distinct written "what could go wrong" step. Cost was real: `scripts/build_demo_engagement.py` has two CLI-arg-selected entry points and the bare-invocation one silently regenerates a smaller, wrong artifact set (see new L-078) — a pre-mortem asking "does this regeneration script have modes, and am I invoking the one the repo actually ships?" would have caught it before running anything. Caught post-hoc instead, by inspecting `git diff --stat` after the fact and noticing an implausible line-count shrink. Sixth build-task skip in a row (after #47/#48/#56/#59/#69); L-067's test-first mitigation and the dispatched verifier continue to be the only things actually catching what this rule is meant to prevent upfront
- observed: 2026-08-27 — SKIPPED a seventh time on issue #87 (Q4 structured poison classification): investigated thoroughly (poisons.json, test_poison_suite.py, build_demo_engagement.py, types.ts, verdicts.ts) before writing any code, but went straight from that investigation into implementation with no distinct written pre-mortem enumerating failure modes. No defect resulted — the adversarial `verifier` agent and a manual tsc-exhaustiveness probe both came back clean — but the directive was again not separately invoked before implementing, same as the prior six. Unlike several earlier instances, the eventual outcome was correct on the first attempt; this weakens the case that skipping this step is costly in practice for well-scoped, well-investigated tasks, even though the rule itself was still not followed as written
- observed: 2026-08-28 — SKIPPED a seventh time on issue #97/PR #101 (Commitment model): went from an Explore agent's investigation report straight into implementation (schema model, build-script derivation, frontend join) with no distinct written adversarial pre-mortem on the join logic itself. Cost was real: the frontend commitment join (`Object.values(commitments).find(...)`) was a silent first-match with no check for two commitments ever citing the same claim id — an ambiguity a two-minute "what if two commitments overlap" pre-mortem would have named upfront. Caught post-hoc instead, by the dispatched adversarial `verifier` agent, which is exactly this rule's own recurring pattern; a `SystemExit` guard was added afterward to close the gap. Seventh consecutive build-task skip (after #47/#48/#56/#59/#69/#78); the one propose-only firing (2026-08-24) remains the sole exception across eight opportunities
- observed: 2026-09-01 — SKIPPED an eighth time on issue #52/PR #117 (reference-engagement population sizes): investigated thoroughly (grepped for where the placeholder numbers actually drive behavior, confirmed no field exists) before editing, but went straight from that investigation into the doc edits — including renumbering `docs/EXECUTION-PLAN.md`'s Owner-open-items list — with no distinct written "what does this renumbering break elsewhere" pre-mortem. Cost was real: a newly authored cross-reference in `docs/HANDOFF.md` ("see §6 item 1") pointed at the wrong entry after a renumbering performed earlier in the same change (see new L-007 observed-note). Caught post-hoc by the dispatched adversarial `verifier` agent, the identical pattern every prior instance of this entry describes. Eighth consecutive build-task skip (after #47/#48/#56/#59/#69/#78/#97); the one propose-only firing (2026-08-24) remains the sole exception across nine opportunities — a candidate rule at Evidence 1 that has now been skipped on every single build task since it was written is not a rule this session type is applying from memory, full stop
- Scope: portable
- Evidence: 1
- Status: inherited (was: promoted:CLAUDE.md (Evidence 1 — promoted on explicit user confirmation) in janus)

## L-009 · 2026-07-06 · Seed behavioral fixtures with controlled data, never the repo's live content
- Trigger: bumping L-005 to Evidence 2 broke two session-start fixtures that asserted against the shipped ledger's real entry counts (janus round-3 session)
- Rule: a behavioral fixture must create the data it asserts against (truncate and seed in the sandbox); asserting against live repo content couples tests to unrelated edits
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-010 · 2026-07-06 · Store cross-clone state in file content, never in file mtimes
- Trigger: the recalibration staleness design first used a gitignored marker checked via find -mtime; gitignored files never sync between clones, and git does not preserve mtimes on committed files either — both halves were broken (janus round-3 session)
- Rule: any timestamp or state that must survive a git clone boundary goes in the file's content (e.g. epoch seconds) and is committed; mtimes are per-checkout artifacts
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-012 · 2026-07-06 · Propose the maintenance heartbeat at the end of /bootstrap
- Trigger: user asked why the weekly heartbeat isn't on by default for every project; auto-creating a billed cloud routine silently would violate the escalation-is-proposed rule, but the end of session zero is the natural moment to offer it (janus round-3 evolve session)
- Rule: when a project finishes bootstrapping, propose creating the maintenance heartbeat (one yes, PR-delivery only) instead of waiting for the user to discover it
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-014 · 2026-07-06 · Mechanisms enter the template only after dogfooding evidence demonstrates the need
- Trigger: round-3.5 subtraction removed Graphify (never built here), the ARCHIVE.md + ≤25 cap (ledger held 12 entries), and staged expiry policies — all machinery added for problems no session had hit, against the repo's own Evidence >= 2 discipline (janus round-3.5 session)
- Rule: disciplines can be designed up front, but a mechanism (tool, file, cap, fallback) enters the scaffold only after real use demonstrates the need it serves
- observed: 2026-08-18 — fired twice in one /evolve: it cleared L-052's write-side hook (Evidence 5, need demonstrated) and refused an L-049 `integrate-worktree.sh` helper (Evidence 1, premature)
- observed: 2026-08-24 — fired at this build heartbeat firing: `docs/lane-fit-notes.md`'s FIT-001..005 entries each end with an explicit extension sketch, several literally noting "deferred until a second lane needs it (L-014)"; when scanning for step-2c proposal candidates, these were considered and rejected as proposals precisely because no second lane exists yet to demonstrate the need — correctly left as recorded evidence rather than built
- observed: 2026-08-27 — fired at issue #94 (Q13, source coverage): considered adding a full JSON Schema + codegen entry for `reconciliation.json` (the issue's own done-means suggested it as one option) but found no such schema exists for that artifact at all today — no dogfooded need has yet forced one into being, and the repo's own established precedent for this exact shape (`EXERCISED_CAPABILITY_IDS`, a hand-maintained id set with a drift-guard, not a schema field) already covers a source→capability reference. Built the drift-guard, not the schema
- observed: 2026-08-28 — fired shaping a proposal issue's own done-means rather than clearing a candidate: filing #97 (Q16 — model Commitment/Requirement so /proof's last two stages can emit), the done-means was written to require scoping the model to exactly what the two placeholder stages need to render (mirroring the manifest snapshot's narrow `genesis()` precedent) and to stop and open a `question:` issue instead of guessing at ontology shape if the PRD-v3:30 sketch proves underspecified — declining to let the proposal itself pre-commit a future firing to inventing scope no dogfooded stage has demonstrated a need for
- observed: 2026-08-28 — fired at issue #97 (Q16 — model Commitment and Requirement): `Commitment` had real defining prose (docs/PRD-v3.md:30) to mirror narrowly and was implemented; `Requirement` had zero defining prose anywhere in the repo (only a stage name + edge label, no paragraph the way every other ontology concept gets one) — rather than invent a plausible-looking shape for it from the ten-stage chain string alone, declined to model it and opened issue #100 asking the Owner to rule on what it actually is first. The issue's own done-means had explicitly named this exact outcome as acceptable, and the firing took it rather than guessing to "finish" the task
- observed: 2026-09-01 (build heartbeat, issue #52/PR #117) — fired: the Owner's ruling on issue #52 gave concrete population numbers (750 employees / 105 terminations / etc.), which could have been read as an instruction to scale the poison-fixture tenant's data up to match. Declined to do so and left `tests/fixtures/poisons/*.json` at its existing 8-termination scale — no poison case or compiler-trap test in the suite needs the larger volume, so scaling it would be a mechanism added against no dogfooded requirement, purely because a number was now available to plug in. Said so explicitly in the PR body rather than silently matching the ruling's numbers everywhere they could technically apply
- Scope: portable
- Evidence: 7
- Status: inherited (was: promoted:CLAUDE.md in janus)

## L-015 · 2026-07-06 · Platform owns mechanisms; the template keeps only the disciplines it adds
- Trigger: the explorer/planner agents duplicated native exploration/planning subagents, and scripts/new-worktree.sh duplicated native claude --worktree; both were removed with their embedded disciplines consolidated into plan-feature and worktree-parallel (janus round-3.5 session)
- Rule: before encoding a mechanism, check whether the platform provides it natively; encode only the discipline the scaffold adds on top, and let the platform's mechanism carry it
- observed: 2026-08-18 — L-045's Rule is literally this rule applied to skill invocation control; at B2 it kept the cwd-drift check inside the existing hook rather than minting a new one
- Scope: portable
- Evidence: 2
- Status: inherited (was: promoted:CLAUDE.md in janus)

## L-016 · 2026-07-06 · Weigh subtraction as seriously as addition when reviewing for improvement
- Trigger: asked "any enhancements?", the audit proposed only fixes and additions; the owner's redirect ("consider features we should remove") produced the round's highest-value changes — four feature removals (janus round-3.5 session)
- Rule: when reviewing a system for improvement, audit what fails to earn its place with the same rigor as what is broken or missing, and propose removals alongside fixes
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-017 · 2026-07-06 · Design from first principles; cite existing implementations as evidence, not as the template
- Trigger: the J-brain memory design was framed "from how contextual memory actually works in current harnesses"; the owner rejected it — "I want to build a harness... what should be designed" — and the first-principles redesign (prediction machine + memory pipeline) superseded it (janus round-3.5 session)
- Rule: when asked to design a system, derive the design from the problem's own invariants; use existing implementations as evidence for or against choices, never as the starting shape
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-018 · 2026-07-06 · Preserve foreign uncommitted changes in their own labeled commit before starting your own
- Trigger: the working tree carried uncommitted docs-consistency fixtures written outside the session (the start-of-session snapshot said clean); committing them unmodified, clearly labeled, before any session work kept authorship legible and the changes safe — they caught a real bug two commits later (janus round-3.5 session)
- Rule: treat uncommitted working-tree changes you did not make as someone else's work — verify what they are, then commit or stash them separately with a label stating their origin, before your own commits touch the tree
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-019 · 2026-07-24 · Name concrete source URLs in an encoded source list, never a category
- Trigger: five new Anthropic posts (claude.com/blog, 2026-07-16..24) had to be hand-delivered by the user because /recalibrate never reached them; the skill names "Anthropic's engineering blog" at SKILL.md:22 but its Hold-in-mind #1 at :14 says "Aggregator and blog claims are leads to verify, never evidence" — the skill contradicts itself, and line 14 silently downgraded L-005's *method* rule (confirm verbatim against a primary source) into a *source-class* rule (blogs are second-class)
- Rule: a source list is executable only if it names concrete URLs; gate credibility on method (verbatim confirmation) rather than on the publisher's format, or whole publications go unread
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-020 · 2026-07-24 · A provenance stamp written by anything but a real run is a false green
- Trigger: .claude/memory/recalibrated-at holds 1783394187 (2026-07-07) written by design commit 539a12e, not by a run — `git log` on the file shows exactly one commit, so /recalibrate has never actually completed in this repo, yet the 30-day staleness nudge reads as satisfied (janus claude-5 realignment session)
- Rule: write a run stamp only from the run it certifies; a stamp set by the commit that designed the mechanism records provenance that never happened and suppresses the very nudge meant to catch it
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-021 · 2026-07-24 · Verify which memory tiers survive the environments you actually run in
- Trigger: code.claude.com/docs/en/memory states verbatim "Auto memory is machine-local. All worktrees and subdirectories within the same git repository share one auto memory directory. Files are not shared across machines or cloud environments" — so this scaffold's headless heartbeat routine and every cloud session start with an empty ambient tier, a consequence the memory-pipeline design never accounted for (janus claude-5 realignment session)
- Rule: for each memory tier a design depends on, confirm which execution environments it actually reaches; a tier that is absent headless or in the cloud cannot carry anything the automation relies on
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-022 · 2026-07-24 · Keep volatile platform facts out of inherited memory, or state their expiry
- Trigger: the claude-5 findings include vendor state that will rot (auto-memory locality, a 1,536-char listing truncation, the current frontmatter field set); /replicate copies every `Scope: portable` entry into every child forever and entries are never deleted, so filing them portable would breed claims with no expiry and no deletion path (janus claude-5 realignment session)
- Rule: file the durable discipline as the portable rule and keep the vendor fact in the Trigger line as dated evidence — an inherited entry must stay true when the platform changes under it
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-023 · 2026-07-24 · A platform's truncation ceiling is not a budget — don't swap a binding discipline for it
- Trigger: this session's own plan proposed replacing the repo's ≤50-word skill-description cap with the docs' "truncated at 1,536 characters in the skill listing"; an adversarial pass caught that the ceiling is where rendering stops, not a target — adopting it would license ~15,360 chars of always-loaded description against ~2,816 today, a 5.5x increase, while quoting "every token added depletes Claude's attention budget" as the warrant (janus claude-5 realignment session)
- Rule: when replacing a scaffold cap with a native limit, check whether the native number is a target or a failure threshold; substituting a ceiling for a binding discipline loosens the constraint while appearing to modernize it
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-024 · 2026-07-24 · Aim a recalibration at the conventions it would be most costly to lose
- Trigger: the claude-5 context-engineering post ("Give Claude rules" -> "Let Claude use judgement", "Repeat yourself" -> "Simple tool descriptions", "Memory in CLAUDE.md files" -> "Auto-memory") challenges this scaffold's own signature mechanisms — the Hold-in-mind ritual, the prime directives, and the manual reflect/evolve pipeline — and the first draft of the realignment plan proposed changes everywhere except there (janus claude-5 realignment session)
- Rule: a recalibration that returns only comfortable findings has not recalibrated — enumerate the conventions the scaffold is proudest of and file the evidence against them by name
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-025 · 2026-07-24 · Spend always-loaded memory on gotchas, not on what the codebase already shows
- Trigger: claude.com/blog's claude-5 context-engineering post says verbatim "Keep your CLAUDE.md lightweight and briefly describe what your repo is for, but spend most of the tokens on gotchas inside of the codebase", and the /doctor trim check "cuts content Claude can derive from the codebase, such as directory layouts, dependency lists, and architecture overviews"; this repo's CLAUDE.md carried a ## Map section of exactly that derivable content and no gotchas at all (janus claude-5 realignment session)
- Rule: in an always-loaded memory file, a line that restates the directory tree is paying permanent rent for something a single `ls` recovers — spend the budget on traps that bite and are invisible from the code
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-026 · 2026-07-24 · Derive a validator's allowlist from the primary source, not from what you just read
- Trigger: the new frontmatter fixture's field sets were written from the fields this session happened to have quoted — it then rejected documented-valid input (`shell:` on a skill; `color:`/`skills:`/`isolation:` on an agent, where 5 of 16 documented fields were known) and its lowercase-only regex made every camelCase field invisible to validation, including misspellings; the adversarial verifier caught all of it against the docs (janus claude-5 realignment session)
- Rule: when a check encodes a set of valid values, build the set by enumerating the primary source in that moment — a validator written from recall is a claim about a moving target that fails in both directions, rejecting what is valid and silently passing what is not
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-027 · 2026-07-24 · Refresh remote-tracking refs before reasoning about what has already landed
- Trigger: `git log --oneline origin/main..HEAD` ran without a prior fetch and reported 12 unmerged commits; all 12 were already on main via PRs #4-#6. A scope decision was put to the user on that count, and the resulting PR opened CONFLICTING against a main that had since landed its own /evolve round, forcing the PR to be closed and rebuilt (janus doctor+evolve session). Distinct from L-005's round-3.5 branch incident: there the remote branch never existed and a report asserted it unverified (a trust-the-summary failure); here the ref existed but the local cache was stale — the primary-source command itself lied
- Rule: run `git fetch` before any command that reads a remote-tracking ref — `origin/*` is a local cache, and a comparison against an unfetched one returns a confidently stale answer, not a visibly wrong one
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-028 · 2026-07-24 · Audit a delegated report's premises, not only its citations
- Trigger: the memory-curator agent's promotion proposal was checked line by line against LEARNINGS.md and every Evidence count held — yet four of its five proposed promotions were already merged on main, because its unstated premise (that the working tree reflected the shared branch) was never tested; the agent was scoped to the local tree and could not have known (janus doctor+evolve session)
- Rule: when auditing a subagent's report, name and test its unstated premises and the scope it could actually observe — correctly verified citations under a false premise still yield a wrong conclusion
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-029 · 2026-07-24 · Pass multi-line command bodies through a file, never inline command substitution
- Trigger: `gh pr create --body "$(cat <<'BODY' ...)"` failed with `unexpected EOF while looking for matching quote`; writing the identical body to a file and passing `--body-file` succeeded unchanged. A heredoc piped directly to stdin (`git commit -F -`) was unaffected — only the nesting inside command substitution broke (janus doctor+evolve session)
- Rule: write multi-line text — PR and issue bodies, JSON payloads, config blocks — to a file and pass it by path; never nest a heredoc inside command substitution inside a quoted argument
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-030 · 2026-07-24 · Derive budgets from operational guidance and dogfooding, never from a lens hyperparameter
- Trigger: ARCHITECTURE claimed the workspace paper "found ~10–25 simultaneously active concepts" and derived the ≤20 cap from it; main-thread verbatim read (methodology review) showed the paper *chooses* "no more than 25" as a J-lens hyperparameter, calls the lens "an imperfect tool", and contains no such range and no claims about instruction files — the external reviewer who praised the derivation had taken the repo's framing at face value (origin: fetched content, main-thread verified; prompted by user-supplied critiques)
- Rule: ground every encoded budget in operational guidance or dogfooded evidence; an interpretability measurement choice is convergent context at most, and citing it as a derivation is numerology
- Scope: portable
- Evidence: 1
- Status: inherited (was: promoted:docs/ARCHITECTURE (Evidence 1 — applied on explicit user confirmation, methodology review) in janus)

## L-031 · 2026-07-24 · A cross-session signal must carry enough context to reconstruct why it fired
- Trigger: correction signals logged only `correction:<timestamp>`; a Stop-hook nudge fired on a leftover signal whose cause could only be guessed at, and the cross-session leftover-signals path offered a bare count with no recoverable context (origin: own observation, this session)
- Rule: any signal a later session may consume must carry its cause — matched keyword, source excerpt, or file path — not just a timestamp; a context-free signal forces the consumer to guess
- Scope: portable
- Evidence: 1
- Status: inherited (was: promoted:hooks/prompt-signal (Evidence 1 — applied on explicit user confirmation, methodology review) in janus)

## L-032 · 2026-07-24 · Git-shared memory is an injection channel — untrusted-origin evidence never promotes unreviewed
- Trigger: repo-wide grep found zero trust boundaries on the ledger → CLAUDE.md → /replicate pipeline while Anthropic names the exact channel: "An injection that lands in any of these is reloaded each time the agent starts" and "Tool output is an attack surface even when the tool is trusted" (how-we-contain-claude, main-thread verified 2026-07-24; origin: fetched content + user-supplied critique)
- Rule: an entry whose evidence originates in untrusted content — fetched pages, tool output, repo text — promotes into always-loaded context only with the user's explicit confirmation, whatever its Evidence count
- Scope: portable
- Evidence: 1
- Status: inherited (was: promoted:skill/evolve (Evidence 1 — applied on explicit user confirmation, methodology review) in janus)

## L-033 · 2026-07-24 · A promoted rule with no observed effect needs a retirement path
- Trigger: retirement fired only on contradiction, so a useless rule had no exit; every loop metric counted compliance (budget assertions, claims-checked tallies), never outcomes — against "you should consider adding complexity _only_ when it demonstrably improves outcomes" (building-effective-agents, main-thread verified; origin: user-supplied critique + fetched content)
- Rule: track when promoted rules visibly fire or fail to help, and propose retirement for rules with no observed effect — compliance counting is not outcome evaluation
- Scope: portable
- Evidence: 1
- Status: inherited (was: promoted:skill/evolve (Evidence 1 — applied on explicit user confirmation, methodology review) in janus)

## L-034 · 2026-07-24 · Evidence units are independent incidents; merges take the max, never the sum
- Trigger: the memory-curator brief instructed "summing their Evidence" across merged near-duplicates — two unrelated anecdotes could manufacture a promotable 2 — and nothing anywhere defined recurrence or barred same-session double-bumps (origin: own observation of repo text, methodology review)
- Rule: an Evidence unit is a distinct incident from a separate session or task; the same event never counts twice, one session bumps an entry at most once, and merged entries take the max of their counts
- Scope: portable
- Evidence: 1
- Status: inherited (was: promoted:skill/reflect (Evidence 1 — applied on explicit user confirmation, methodology review) in janus)

## L-035 · 2026-07-24 · Rules crossing a generation boundary get human review before landing active
- Trigger: /replicate copied promoted rules straight into every child's always-loaded rules block while USAGE promised children "re-earn promotion" — a contradiction that left the generational injection path Anthropic warns about ungated; ledger census at review time: 29/29 entries marked portable (origin: own observation of repo text + fetched content)
- Rule: a rule that will land active in a descendant's always-loaded context requires the user's explicit yes at the generation boundary; inherited ledger entries stay inactive until re-earned
- Scope: portable
- Evidence: 1
- Status: inherited (was: promoted:skill/replicate (Evidence 1 — applied on explicit user confirmation, methodology review) in janus)

## L-036 · 2026-07-24 · A refutation is a claim — verify absence with substring rigor, not sentence matching
- Trigger: a delegated verifier reported "Once an agent discovers a bug class, the relevant file is updated to prevent it recurring" as a fabricated quote because its exact-string check matched a punctuation-terminated sentence against text that continues "…in future code."; a raw-HTML substring fetch proved the sentence real (origin: subagent report, falsified by own observation, methodology review)
- Rule: verify a claimed absence with the same rigor as a claimed presence — match substrings against raw source, never full sentences with terminal punctuation, and treat a delegate's refutation as unverified until reproduced
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-037 · 2026-07-24 · The self-learning claim is unproven until a bootstrapped child measures outcomes
- Trigger: methodology review adopted the critique that nothing measures whether promoted rules reduce failures; the full baseline-vs-scaffold evaluation was deliberately deferred — the template has no real coding tasks to measure (origin: user-supplied critique, user decision 2026-07-24). Re-open trigger: the first bootstrapped child with real task history. Protocol sketch preserved: fixed task set; arms = plain Claude Code / verify-only / memory-only / full scaffold; measure task success, repeated-error rate, interventions, tokens; test longitudinally (task N benefits from lessons of tasks 1..N-1)
- Rule: before claiming a learning loop improves outcomes, run the deferred baseline comparison in a bootstrapped child — efficacy notes on promoted rules are the interim signal, not the proof
- Scope: portable
- Evidence: 1
- Status: inherited (was: candidate in janus)

## L-038 · 2026-07-24 · Gate the commit on the verifier's exit in the same command chain
- Trigger: a commit ran as an unconditional statement after the verify invocation in one compound command; the suite was red (a component-map fixture) and the red commit landed, needing an amend after the fix (origin: verify failure, own observation, this session)
- Rule: never pipe a gating command's output before using its exit status — run the gate bare (redirect to a file if output is needed) or set pipefail in the same shell — then chain the commit with && on that status; sequential statements commit red results, and a pipe makes the status the last stage's, not the gate's
- Scope: portable
- Evidence: 2 (the red commit that landed as a sequential statement, janus 2026-07-24; `verify.sh full 2>&1 | tail -4 && git commit` laundering the exit code through tail, aegis 2026-08-16 — two independent incidents in separate sessions, merged from L-050 whose own title recorded it as "evidence of L-038 recurring")
- observed: 2026-08-18 — fired throughout the B2 session: every gate ran bare into a log file with `echo EXIT=$?`, and no exit code was laundered
- Status: candidate — RIPE, absorbed L-050's wording 2026-08-18 on user confirmation

## L-039 · 2026-08-16 · A work order's done-means check must be executable inside the agent's allowlist
- Trigger: the WO-C2 issue-template run (6d0d805) needed a YAML parse to verify its own output, no allowlisted command could do one, and the dispatched agent burned ~50 turns on 35 permission denials before dying on max-turns; the fix (64b13c5) put the YAML check inside scripts/verify.sh. The lesson was written as prose comments in verify.sh and tests/test_issue_templates.py but never entered this ledger — backfilled by the 2026-08-16 memory audit (origin: verify failure, own observation)
- Rule: before dispatching a work order, confirm every done-means check is runnable via an allowlisted command — verification that needs an unlisted tool becomes a denial loop, not a closed loop
- Scope: project
- Evidence: 1
- Status: promoted:skill/recalibrate — resolved; SKILL.md now reads \"The gate is method, not publisher: any source can be primary\" (ledger status was stale)

## L-040 · 2026-08-16 · A mechanical template copy leaves the memory loop wired but dead — run the provisioning ritual
- Trigger: this repo was stamped from janus on 2026-07-27 by template copy, not /replicate; every heredity transform was skipped (parent statuses and retired entries crossed, sources-seen watermark intact, identity unrewritten), and in 3 weeks of real work no session ran /reflect, /evolve, or /recalibrate despite the Stop hook and session-start nudging every session — real lessons were routed into code comments instead of the ledger (origin: own observation, 2026-08-16 memory audit; retroactive fix in this branch)
- Rule: when adopting a scaffold whose skills assume a provisioning ritual, run the ritual's transforms — or apply them retroactively before real work continues; nudges alone do not revive a loop that provisioning left dead
- Scope: project
- Evidence: 1
- Status: candidate

## L-041 · 2026-08-16 · The skill-frontmatter authority is the Agent Skills spec at agentskills.io, with Claude Code extensions on top
- Trigger: /recalibrate 2026-08-16 — github.com/anthropics/skills spec/agent-skills-spec.md (the authority add-skill and recalibrate encode) now redirects to agentskills.io/specification; the standard defines only {name, description, license, compatibility, metadata, allowed-tools} while the Claude Code docs state verbatim "Claude Code extends the standard with additional features" and document when_to_use, argument-hint, disable-model-invocation, disallowed-tools etc. as product fields (https://agentskills.io/specification, https://code.claude.com/docs/en/skills)
- Rule: when validating or citing skill frontmatter, name the standard and the product extension separately — a field valid in Claude Code may be rejected by spec-side tooling, and the spec's location is itself a moving target
- Scope: portable
- Evidence: 1
- Status: candidate

## L-042 · 2026-08-16 · Encode "unused by policy" as a discipline with a doc pointer, never a frozen enumeration of a growing surface
- Trigger: /recalibrate 2026-08-16 — ARCHITECTURE's "Deliberately unused events: PreToolUse, PreCompact, Notification, SubagentStop" reads as the full set, but the hooks reference now lists ~31 events including InstructionsLoaded ("When a CLAUDE.md or .claude/rules/*.md file is loaded into context"), WorktreeCreate/Remove, PostCompact, SubagentStart (https://code.claude.com/docs/en/hooks)
- Rule: when a convention names the members of a platform surface, it rots as the surface grows — encode the policy ("only these events are used, add hooks only with a purpose") and point at the platform's list instead of copying it
- Scope: portable
- Evidence: 1
- Status: candidate

## L-043 · 2026-08-16 · Block-level HTML comments in CLAUDE.md are stripped before injection — sentinel markers cost zero context
- Trigger: /recalibrate 2026-08-16 — memory docs state verbatim "Block-level HTML comments (`<!-- maintainer notes -->`) in CLAUDE.md files are stripped before the content is injected into Claude's context" (https://code.claude.com/docs/en/memory); the scaffold's janus:facts/janus:rules sentinels are exactly such comments, so the budget accounting can stop treating them as spent lines
- Rule: in an always-loaded memory file, machine markers and maintainer notes are free when written as block-level HTML comments — spend visible lines only on content the model must read
- Scope: portable
- Evidence: 1
- Status: candidate

## L-044 · 2026-08-16 · Auto mode is the platform default — broad allow rules are set aside, so guarantees must ride on narrow runner-shaped rules
- Trigger: /recalibrate 2026-08-16 — claude.com/blog states verbatim "Starting on August 14, new sessions on Pro, Max, and Team plans will run in auto mode" and "Permission rules still fire before the classifier in auto mode, except for allow rules broad enough to grant arbitrary code execution" (https://claude.com/blog/auto-mode-default-in-claude-code); this session itself hit the auto-mode classifier, and the repo's single narrow allow rule (Bash(scripts/verify.sh *)) kept working
- Rule: design committed allowlists as narrow, runner-shaped rules — in auto mode a broad allow rule is suspended, so any workflow guarantee that depends on one silently stops holding
- Scope: portable
- Evidence: 1
- Status: candidate

## L-045 · 2026-08-16 · Side-effect skills can declare disable-model-invocation — the platform now carries the operator-timing gate
- Trigger: /recalibrate 2026-08-16 — skills docs document verbatim a `disable-model-invocation` field ("Set to `true` to prevent Claude from automatically loading this skill... for workflows with side effects or that you want to control timing, like /commit, /deploy") plus `disallowed-tools` (https://code.claude.com/docs/en/skills); the scaffold's side-effect skills (ship, replicate, evolve) encode that timing discipline only as in-body confirm gates
- Rule: where the platform grows a native mechanism for an encoded discipline, propose migrating the mechanism and keep only the judgment the scaffold adds (L-015 applied to skill invocation control) — adoption is an /evolve decision, not a silent edit
- Scope: portable
- Evidence: 1
- Status: candidate

## L-046 · 2026-08-16 · /evolve routes each lesson to the highest enforceable rung, not to prose by default
- Trigger: user correction 2026-08-16 during this repo's memory audit — "why would CLAUDE.md just track these but not build a solution to fix these?"; this repo is the dogfooded case: it already builds mechanisms where a core is checkable (docs-consistency fixtures, redaction gate, purity tests) while /evolve's routing knew only prose targets (origin: user correction; canonical entry janus L-040, applied to the shared skills here the same day)
- Rule: route every promotion up an enforcement ladder — hook/fixture, then verifier check, then skill, then path rule, then CLAUDE.md prose as last resort — and re-ask on recurrence whether a promoted prose rule has revealed a checkable core
- Scope: portable
- Evidence: 1
- Status: promoted:skill/evolve (Evidence 1 — applied on explicit user confirmation, 2026-08-16)

## L-047 · 2026-08-16 · Make judgment disciplines auditable: document the ritual, presence-check mechanically, judge substance independently
- Trigger: user correction 2026-08-16 re inherited L-008 — "can't there be an agent log … so an independent verify can confirm this happened?"; plan-feature required the Predicted-failure-modes artifact but nothing independent confirmed the ritual ran (origin: user correction; canonical entry janus L-041). observed: 2026-08-16 — the verifier's WALKING SKELETON audit flagged the absent failure-modes trail unprompted (roadmap-driven change, no /plan-feature run), correctly stopping short of a process FAIL while naming the gap — the rule fires as designed
- Rule: for a judgment discipline, require a named artifact of the ritual, then split enforcement — mechanical presence-check where possible, verifier-agent judgment of substance always — so compliance stops depending on the actor's own report
- Scope: portable
- Evidence: 1
- Status: promoted:skill/plan-feature+agent/verifier (Evidence 1 — applied on explicit user confirmation, 2026-08-16)

## L-048 · 2026-08-16 · A merged milestone is a trigger, not a stopping point — surface the next task mechanically
- Trigger: user correction 2026-08-16 — after SCH01 merged the session reported "say the word" while docs/EXECUTION-PLAN.md named SCH02 as next; the Conduct-don't-wait directive was prose with no mechanism (origin: user correction; canonical entry janus L-042, hook mirrored here the same day)
- Rule: when a repo carries an execution plan, the harness surfaces the first unticked task at session start and the session continues it — continuation is the harness's job, not the user's prompt
- Scope: portable
- Evidence: 1
- Status: promoted:hooks/session-start (Evidence 1 — applied on explicit user confirmation, 2026-08-16)

## L-049 · 2026-08-16 · Mirroring a shared scaffold file between repos by blind copy clobbers repo-specific divergence
- Trigger: two incidents this session — copying janus's SELF-IMPROVEMENT.md over aegis-sentinel's reverted the corrected CI wording (caught by own L-007 sweep), and copying janus's test-hooks.sh deleted this repo's *.yml quick-arm fixtures undisclosed (caught by the adversarial verifier post-commit); both from `cp` of a diverged file (origin: verify failure + verifier report, confirmed against git history)
- Rule: before mirroring a scaffold file across repos, diff it against the destination and port the change as a patch — a diverged file is never safely replaced whole
- Scope: portable
- Evidence: 1
- Status: candidate

## L-050 · 2026-08-16 · Piping verify output through tail launders the exit code — evidence of L-038 recurring [RETIRED — merged into L-038]
- Trigger: `verify.sh full 2>&1 | tail -4 && git commit` committed and pushed a red suite this session — the pipeline's exit status is tail's, not verify's, so the && gate held a door that was already open; caught one command later and amended (origin: verify failure, own observation). observed: 2026-08-16 — L-038 ("gate the commit on the verifier's exit in the same command chain") fired in spirit but its rule assumed the exit code reaches the chain; a pipe breaks that assumption
- Rule: (superseded — this wording now lives in L-038, which this entry's evidence bumped to 2)
- Scope: portable
- Evidence: 1
- Status: retired — merged into L-038 (same rule, refined wording); /reflect should have bumped L-038 rather than appending a twin

## L-051 · 2026-08-16 · Worktree subagents deliver untracked files — integrate by clobber-checked copy, never by branch merge
- Trigger: the first collector-wave integration ran `git merge worktree-agent-...` and got a silently empty merge — worktree subagents are told not to commit, so their branches carry no commits and the deliverables sit untracked in the worktree directory; the working pattern (repeated 6x this session) is: list the worktree's `git status --porcelain` untracked paths, refuse any path that already exists in the destination (L-049's clobber guard), copy the rest, re-verify in the main checkout (origin: own observation, verify failure)
- Rule: instruct worktree subagents to commit in their worktree and integrate by merging their branch (gitignore then filters node_modules-style noise); for a subagent that did not commit, fall back to copying its untracked files with a per-path exists-check — merging an uncommitted worktree's branch integrates nothing. Either way, re-run the full suite in the destination
- Scope: portable
- Evidence: 2 (empty-merge failure at the collector wave; instruct-commit-then-merge worked cleanly for A1, MCP01, and A2+B1)
- Status: promoted:skill/worktree-parallel — step 3 now instructs each track to commit in its worktree, step 5 carries the empty-merge check and the clobber-checked-copy fallback

## L-052 · 2026-08-16 · The shell's working directory between tool calls is undefined — anchor every path
- Trigger: four separate commands this session failed with "not a git repository" / "No such file or directory" because the session cwd had silently reverted to the home directory between calls (worker restarts and environment reconnects reset it); each failure cost a retry with an explicit cd (origin: own observation, repeated)
- Rule: treat the working directory between tool calls as undefined — it may reset OR persist, and believing it always resets is itself how `web/web/src/...` gets written. Start every compound shell command with an absolute-path cd (or use absolute paths throughout), and never assume the previous call's working directory either survived or did not; and within one compound command, every segment after a `cd` inherits it, so re-anchor before each phase that needs a different directory (web gates in `web/`, repo scripts at the root)
- Scope: portable
- Evidence: 5 (four cwd-reset failures early in that session; three conductor passes then ran `scripts/verify.sh` from `web/` inside a compound command — exit 127 each time, twice AFTER this rule was written; recurred 2026-08-18 in the B2 session in the OTHER direction — cwd PERSISTED from a prior `cd web`, so two heredoc writes landed at `web/web/src/...` and one `cat > web/src/app/...` failed outright. The promoted mechanism covers gate RUNS, not file WRITES: `verify-web.sh` self-anchors, but a heredoc path does not)
- Status: promoted:hooks/post-edit-verify + scripts/verify-web.sh — two halves, because one mechanism cannot see both. RUN side: `verify-web.sh` self-anchors and runs all frontend gates + redaction from any cwd. WRITE side (added 2026-08-18 after the B2 recurrence): `post-edit-verify.sh` rejects a written path that repeats an adjacent directory segment — the fingerprint of a relative path resolved from a drifted cwd — with six fixtures in `scripts/test-hooks.sh` including a `Dir/Dir.ext` false-positive guard. A self-anchoring script cannot anchor a heredoc path, which is why prose alone kept failing. The re-anchor rule stays as the judgment neither mechanism carries: the hook never sees a `cd` inside a Bash command.

## L-053 · 2026-08-16 · `as T` on JSON imports is a vacuous per-record check — wire JSON through an assignability position and prove it with a falsifier
- Trigger: the A2+B1 adversarial verifier renamed a required key inside a verdict record of an imported JSON array and `tsc --noEmit` stayed green — `index.ts` wired every JSON file through `as T` casts, and TypeScript `as` checks only top-level bidirectional comparability, so a broken record inside any array is swallowed; the doc comment and commit message both claimed "tsc checks every JSON file" (origin: verifier FAIL, prescribed falsifier). observed: 2026-08-18 — fired as designed at B2: after `ReconciliationReport` gained four fields and a sibling array export was added, the prescribed falsifier (rename `member_ref` in one delta) still failed tsc with TS2345, confirming the assignability position survived the edit
- Rule: never claim the compiler checks imported JSON unless the JSON flows through an assignability position — a plain annotation or a generic parameter (`checked<T>(json: Widen<T>)`, literal unions widened to primitives since JSON inference widens strings) — and prove the wiring by making a representative deep mutation fail the build before shipping the claim
- Scope: portable
- Evidence: 1
- Status: candidate

## L-054 · 2026-08-18 · Commit the baseline before running revert-based falsifier probes
- Trigger: the B2 deletion-falsifier run reverted each probe with `git checkout -- <mock>.json`, which discarded the SAME task's uncommitted edits to that file (four keys the emitter change had just added); probes 2–5 then ran against a half-reverted mock and all failed the build with a type error that read like a real defect, costing a full diagnostic detour before the cause — my own revert — was spotted. Committing the verified baseline first made all seven probes pass unchanged (origin: own observation, verify failure). Recurred at C1 (separate task, same session): falsifier F1 mutated types.ts and reverted with `git checkout -- types.ts` BEFORE the C1 commit existed, silently discarding the uncommitted Severity/Q8/optionality alignment; caught because the file vanished from `git status` rather than by any failure — the quiet variant is worse than B2's loud one
- Rule: a falsifier probe whose undo is `git checkout --` can only restore committed state, so commit (or stash) the work under test before the first probe — and when a probe fails in a way the mutation cannot explain, suspect the harness before the code
- Scope: portable
- Evidence: 4 (B2 probe run; C1 falsifier F1; issue #54's adversarial `verifier` subagent; issue #94's adversarial `verifier` subagent — four independent incidents)
- Status: candidate — RIPE
- observed: 2026-08-22 — recurred a third time, this time in a subagent rather than the main thread: verifying issue #54's PR, the `verifier` agent mutated `scripts/build_demo_engagement.py` to test whether its new unit test would fail, then reverted with `git checkout -- scripts/build_demo_engagement.py` — which discarded the main thread's entire uncommitted feature diff on that file (it was never committed, since the PR hadn't been opened yet). The agent caught its own mistake via `git diff --stat` before handing back a verdict and reconstructed the diff from its own transcript, but this shows the rule applies to any agent running revert-based probes against uncommitted work, not just the main thread — a subagent briefed to "verify X" has no reason to know a file is uncommitted unless told, so the brief for any mutation-probe task should say so explicitly
- observed: 2026-08-27 — recurred a fourth time on issue #94, and this time the agent's own catch-before-reporting (as in the 2026-08-22 recurrence) did NOT happen: the `verifier` prompt for the new `capability_id` feature literally instructed "temporarily removing/commenting the line... then revert (git checkout the file)" without flagging that the file already carried the main thread's own uncommitted, not-yet-committed feature changes — the agent ran `git checkout` on `scripts/build_demo_engagement.py`, which restored it all the way to `origin/main`, silently erasing every one of the main thread's real edits (the `SOURCE_CAPABILITY_IDS` mapping, the `capability_id` field, the registry drift-guard) along with its own one-line mutation. Caught only by chance: a proactive system-reminder fired on the next file read announcing the file had "changed on disk", and `git status`/`grep` confirmed zero diff remained. Rebuilt the three edits from the earlier session context and re-verified clean. The main thread had read this exact entry's own prior recurrence before dispatching the agent and still wrote a prompt instructing the unsafe pattern — reading the rule is not the same as applying it when authoring a delegate's instructions

## L-055 · 2026-08-18 · Prerendered HTML is one line — `grep -c` cannot count occurrences in it
- Trigger: the B2 probe harness measured rendered signals with `grep -c`, which counts matching LINES; Next.js prerenders each route as a single-line HTML file, so every signal reported 0 or 1 regardless of how many times it appeared — two probes (source deletion, exclusion deletion) looked like no-ops until re-measured with `grep -o … | wc -l`, which showed the real 5→0 and 1→0 transitions (origin: own observation during falsifier verification)
- Rule: when asserting against minified or prerendered single-line output, count occurrences with `grep -o <pattern> | wc -l`, never `grep -c`; and rebuild before measuring, since a stale artifact from the previous probe reads as the current baseline
- Scope: portable
- Evidence: 1
- Status: candidate

## L-056 · 2026-08-18 · Deletion falsifiers cannot catch authored prose about state — mutate the enum too
- Trigger: B2 shipped seven passing deletion probes, and the adversarial verifier still found two real defects by mutating a state VALUE instead of removing data: `after_dispositions: "RATIFIED"` made the page render "RATIFIED holds" and "RATIFIED not reached" simultaneously (the second hardcoded inside the sentence advertised as computed), and the legal state `STALE` made every rung read "not reached" and silently dropped the `aria-current` landmark, with tsc and the build green. Deleting data can never flip a hardcoded claim about a state the data never takes (origin: verifier report, confirmed by own re-run)
- Rule: pair every deletion falsifier with a value falsifier — set each enum-valued field to every other member its schema permits, not just the one the fixture emits — and where a rendered sentence asserts a state, derive it from that field or make the unhandled case a compile error (an exhaustive `switch` with a `never` arm), never a literal
- Scope: portable
- Evidence: 1
- Status: promoted:agent/verifier + CLAUDE.md (Evidence 1 — applied on explicit user confirmation, 2026-08-18); the probe procedure lives in the verifier's step 2, its plan-time half is the fourth adversarial axis on the L-008 bullet

## L-057 · 2026-08-18 · Manual falsifier probes prove a page once; only a committed test keeps it proven
- Trigger: five consecutive frontend rounds (A3, A4, A5, B2 twice over, in two independent sessions) were verified by an adversarial subagent deleting a seed/artifact row, rebuilding, and diffing the built HTML — every round PASSed and every round's verifier closed with the same uncovered finding: nothing automated guards the result, because `verify.sh full` is Python-only and `verify-web.sh` is tsc + build + redaction (origin: verifier reports, four rounds running). Recurred 2026-08-20 (separate session, issue #43's verdict-tinted-process-graph): the verifier proved the new tint/gate join live (flip a record's status, delete the one compile error, rebuild, diff the static HTML, restore) and closed with the identical uncovered finding — no committed re-runnable script, so a future regression here is caught only if another human/agent repeats the manual probe
- Rule: when the same manual verification is re-performed every round, the repetition is the evidence it belongs in the suite — promote the probe to a committed test (parse the seed/artifact, assert every row and computed count appears in the built HTML) rather than re-running it by hand
- Scope: portable
- Evidence: 5 (A3, A4, A5, B2, and issue #43 verifier reports each flag it independently)
- Status: candidate — the ladder rung is a build-output assertion test wired into `verify-web.sh` and web-verify CI

## L-058 · 2026-08-18 · Two sessions picking "the next unticked box" will build the same box — claim it visibly before starting
- Trigger: the daily heartbeat Routine fired into a fresh session that read the same plan, picked B2, built it, and merged it as PR #37 while this long-running session was independently building B2 for PR #38; the duplicate surfaced only as `mergeable_state: dirty` at merge time, after a full build + adversarial-verify cycle had been spent on the losing implementation, and both sessions had already appended a colliding `L-054` to the ledger (origin: own observation, merge conflict)
- Rule: before starting a plan box, check for an in-flight claim on it (open PRs and unmerged remote branches) — and when starting, make the claim visible on the remote; a plan read from `main` is a snapshot, not a lock, and local memory does not travel between machines
- Scope: portable
- Evidence: 1
- Status: promoted:hook — `session-start.sh` now pairs the next-box line with unmerged-remote-branch detection (bounded `ls-remote`, offline-safe), so the collision is announced at session start instead of at merge. observed: 2026-08-20 — fired as designed: the hook's unmerged-branch line sent this firing to inspect `claude/next-build-priorities-nqfzg0` and `claude/janus-memory-skills-eval-vtnp3y` before picking work, which is what surfaced the stranded-branch problem in L-059 rather than a silent B3 duplicate

## L-059 · 2026-08-20 · A branch reused after its PR merges stops being visible to the claim-check — cut a fresh branch per task, always
- Trigger: own observation during the claim-check step — `claude/next-build-priorities-nqfzg0` and `claude/janus-memory-skills-eval-vtnp3y` each had their PR (#37, #39 respectively) merged, then MORE task commits (B3, B3-hardening, B4, C1, C2, C3) were pushed onto the same branch names afterward instead of cutting new branches per step 1; those later commits were never included in any PR and sat unmerged for over a day across multiple 4h heartbeat firings — correctly not duplicated (L-058's claim-check held), but also never delivered, since "open PR" and "claimed unmerged branch" are the only two states the protocol checks for, and "unmerged branch with no open PR, containing real finished work" is a third state nothing was watching for. Recovered this firing only by treating the stray branch as archaeology: diffing it against every already-merged PR to determine which commits were genuinely undelivered, merging main, independently verifying, and shipping it as PR #40
- Rule: never push a new task's commits onto a branch whose PR has already merged or closed — step 1's `git checkout -B claude/<task-id>-<slug> origin/main` is per-task, not per-session, so re-run it fresh even when continuing work that feels related. And when the claim-check finds an unmerged `claude/*` branch with no corresponding open PR, don't just skip it as "claimed" — check whether its PR already merged for earlier content and it was silently reused: if so it's stranded finished work to recover (merge base, verify, PR), not live in-flight work to avoid
- Scope: portable
- Evidence: 1
- Status: candidate

## L-060 · 2026-08-20 · A red check on a PR may be an undocumented third-party integration, not the repo's own CI gate — verify which before reacting
- Trigger: own observation — PR #40 showed a failing `Vercel` commit status while the repo's actual documented CI contract (`scripts/verify.sh full` + `scripts/verify-web.sh`, mirrored by GitHub Actions `verify`/`web-verify` per CLAUDE.md) was fully green; grepping the repo found no `vercel.json` and no docs mentioning Vercel, and the check didn't exist on either of the two prior merged PRs (#37, #39), confirming it was a newly-installed GitHub App integration whose config lives in a dashboard this session has no token for
- Rule: before treating a red PR check as work to fix, confirm it's part of the repo's own documented CI contract (grep `CLAUDE.md`/`docs/` for the tool, check for its config file in-repo) — an undocumented third-party status with no in-repo config is very likely an Owner-side dashboard setting; don't guess at a fix by adding speculative config files, and don't hold the PR on it. File an Owner-action issue naming the specific setting suspected, note it on the PR, and let the documented gate be the delivery bar
- Scope: portable
- Evidence: 1
- Status: candidate

## L-061 · 2026-08-12 (harvested 2026-08-20) · Verify editable installs resolve outside pytest on macOS
- Trigger: walking-skeleton build on the stranded `feat/web-flow-redesign` branch (never merged) — pip's `__editable__.*.pth` got UF_HIDDEN re-applied within seconds on macOS, and Python 3.14's `site` skips hidden `.pth`, so `import aegis_sentinel` silently failed outside pytest while the suite stayed green (pytest `pythonpath` masked it). Harvested into main's ledger per issue #31 (the branch's own PR never landed, so this entry never reached main until now)
- Rule: after `pip install -e .` on macOS, run a bare `python -c "import <pkg>"` outside pytest; if it fails with the package present, check the venv's `.pth` for the hidden flag and symlink the package into site-packages as the workaround
- Scope: portable
- Evidence: 1
- Status: candidate

## L-062 · 2026-08-20 · A background verifier gates the PR, not the commit — commit once local checks are green
- Trigger: this heartbeat session ran local verify (`scripts/verify.sh full`, `verify-web.sh`) green, then launched a background adversarial `verifier` agent and intended to wait for its report before touching git; the environment's Stop-hook (`stop-hook-git-check.sh`) fired mid-wait demanding uncommitted changes be committed and pushed, forcing a decision before the agent had returned (origin: own observation, this session); recurred 2026-08-21 in two independent firings: issue #47/PR #49 (local verify green, background verifier launched, stop-hook forced a commit+push mid-wait before the agent returned; the rule held, and the verifier's one finding, see L-065, landed as a clean follow-up commit) and issue #53/PR #56 (same shape — committed and pushed per this rule, then pushed a small follow-up commit once the verifier's one finding, a missing negative-path test, came back), confirming the "cheap to follow up" premise in practice twice over, not just in theory
- Rule: once your own manual verify commands are green, commit and push immediately rather than blocking indefinitely on a background verifier agent — the commit is cheap to follow up with a fix commit if the verifier finds something; gate the real "claim done" moment (opening the PR, per CLAUDE.md's prime directive on closed loops) on the verifier's findings instead, not the commit
- Scope: project — assumes an environment stop hook that forces action on uncommitted changes; the underlying principle (don't let an in-flight async check block a cheap, reversible local step) is portable but not yet proven outside this harness
- Evidence: 4
- Status: candidate
- observed: 2026-08-27 — recurred a fourth time (issue #87/PR #91 firing): local verify (`scripts/verify.sh full`, web `tsc`/`build`) was already green, but a background adversarial `verifier` agent was launched and awaited before committing anyway — the Stop hook fired exactly as this entry predicts, demanding commit before the agent had returned. The rule is known and correct but wasn't applied proactively; same meta-problem L-069 already names (a ledger entry's presence doesn't mean it gets consulted before the action it warns against).

## L-063 · 2026-08-20 · This container's default python3 is 3.11; the project needs 3.12 — a venv must be created and activated before verify.sh, and even then verify which interpreter you actually got
- Trigger: own observation — `pip install -e '.[dev]'` against the ambient `python3`/`pip` failed with "Package 'aegis-sentinel' requires a different Python: 3.11.15 not in '>=3.12'" even though `/usr/bin/python3.12` was already present on the image; `scripts/verify.sh` shells out to bare `python3`/`pip`/`pytest`/`ruff` with no venv logic of its own, so `full` silently collected zero real tests (21 `ModuleNotFoundError` collection errors for pydantic/jsonschema, read at first as a broken repo) until a `python3.12 -m venv .venv` was created and activated ahead of the install
- Rule: in this repo's remote container, before running `pip install -e '.[dev]'` or `scripts/verify.sh full`, create (or reuse) a `.venv` via `python3.12 -m venv .venv` and `source .venv/bin/activate` first — never assume ambient `python3` already satisfies the `>=3.12` requirement, and read a wall of `ModuleNotFoundError` collection errors as a missing-install signal before assuming the suite itself is broken. Creating the venv through `python3.12` is not sufficient on its own: verify `bin/python3 --version` inside the new venv (or just invoke `bin/python3.12` explicitly for every command) before trusting it, since the venv's own `bin/python3` symlink can still resolve to the container's ambient 3.11
- Scope: project
- Evidence: 7
- Status: candidate
- observed: 2026-08-21 — recurred in a sharper form (issue #47/PR #49): `python3.12 -m venv /tmp/venv-aegis` succeeded and `pip install -e '.[dev]'` through that venv's own `python3`/`pip` reported success (packages installed, wheel built, zero errors) — but `python3 -c "import aegis_sentinel"` then failed with `ModuleNotFoundError`. `sys.path` showed `/tmp/venv-aegis/lib/python3.11/site-packages`: the venv's `bin/python3` symlink pointed at `/usr/local/bin/python3` (3.11), not at the 3.12 binary that had just created it. This is worse than L-063's original case (an upfront, loud version-mismatch error) because the install itself reports zero errors — recreating the venv and invoking every command through `bin/python3.12` explicitly fixed it on the first try. Extended the Rule above with the explicit-interpreter-or-verify step this case needed
- observed: 2026-08-23 — recurred a third time (issue #64/PR #66): bare `python3 -m venv .venv` + `pip install -e '.[dev]'` failed with the same "requires a different Python: 3.11.15 not in '>=3.12'" message on the very first try; `python3.12` was present at `/usr/bin/python3.12` as before, and recreating the venv from that interpreter fixed it immediately. Third independent session hitting the identical failure mode from a cold container — the rule holds and the fix is now routine, but nothing in `scripts/verify.sh` or a setup script encodes it yet, so every fresh firing pays this same detour
- observed: 2026-08-25 — recurred a fourth time (issue #73 firing): ran `./scripts/verify.sh full` first, without a venv, and got the expected wall of 21 `ModuleNotFoundError` collection errors (jsonschema, pydantic, etc.); recognized it immediately per this entry's own rule rather than treating it as a broken suite, ran `python3.12 -m venv .venv && source .venv/bin/activate && pip install -e '.[dev]'`, and the full suite passed cleanly on the very next run. The workaround is now reliable and fast to apply, but the detour itself (one full `verify.sh full` run wasted before remembering to set up the venv) still happens on cold containers because nothing in `scripts/verify.sh` checks or creates the venv itself — this entry's closing observation from 2026-08-23 still holds
- observed: 2026-08-27 — recurred a fifth time (issue #86/PR #89 firing): `pip install -e '.[dev]'` against ambient `python3` failed with the identical "requires a different Python: 3.11.15 not in '>=3.12'" message on the first try; `python3.12 -m venv .venv && source .venv/bin/activate && pip install -e '.[dev]'` fixed it immediately and `./scripts/verify.sh full` then passed (246 tests). Fifth independent session paying this same cold-container detour — still nothing in `scripts/verify.sh` or a setup hook creates the venv automatically, so this remains a strong `/evolve` candidate (a `SessionStart` hook or a venv-aware `verify.sh` wrapper would eliminate the detour outright)
- observed: 2026-08-27 — recurred a fifth time (issue #87 firing): `python3 -c "import aegis_sentinel"` on the ambient interpreter failed with the expected `ModuleNotFoundError`; recognized immediately per this entry's rule, ran `python3.12 -m venv .venv && source .venv/bin/activate && pip install -e '.[dev]'`, and everything worked on the first try — cheapest recovery yet (one wasted probe command instead of a full failed `verify.sh full` run), but the underlying detour still recurs every cold-container firing since no setup script encodes the fix
- observed: 2026-08-28 — a new failure mode in the same family (issue #97/PR #101): created `.venv312` correctly via `python3.12 -m venv`, confirmed it worked, then `mv .venv312 .venv` purely to match the repo's gitignored convention — this silently broke every installed console-script (`pytest`, etc.), because pip bakes the venv's *absolute path* into each script's shebang line (`#!/home/user/aegis-sentinel/.venv312/bin/python3.12`) at install time; renaming the directory afterward leaves every shebang pointing at a path that no longer exists, so `scripts/verify.sh full` failed with a bare "cannot execute: required file not found" that gave no hint the venv itself was otherwise fine. `.venv/bin/python` (the interpreter binary itself, no shebang involved) still worked throughout, which is what made the cause non-obvious. Fixed by deleting and recreating the venv directly at the final path rather than renaming into place — if a venv must live at a specific path, create it there from the start; never `mv`/rename a venv after `pip install` has run inside it

## L-064 · 2026-08-20 · The heartbeat prompt's fallback skill path (.claude/skills/work-loop/SKILL.md) doesn't exist in this repo
- Trigger: own observation — the scheduled build-heartbeat prompt's step 2b says "fall back to ONE ready `task:` issue per `.claude/skills/work-loop/SKILL.md`" when every EXECUTION-PLAN box is ticked/claimed/blocked; `Glob .claude/skills/**/SKILL.md` lists ten real skills and no `work-loop` among them, so the referenced readiness definition has to be reconstructed from the prompt's own inline gloss ("carries a done-means, is inside this environment's tool grant, and is not blocked by an unanswered `question:` or a `loop:hold` label") rather than read from a file; recurred 2026-08-21 — a fresh firing (issue #53) independently re-confirmed the same path is still missing (`Glob`/`ls .claude/skills` both came up empty for it) and again fell back to the prompt's own inline gloss without stalling; recurred a third time 2026-08-22 (issue #58 firing) — `Glob .claude/skills/work-loop/**` and `git ls-tree -r origin/main` both confirmed the path is still absent from main, and the inline gloss was applied again to pick between two ready issues without stalling
- Rule: when a routine's stored prompt names a skill/doc path as the source of a rule, verify the path exists before relying on it — if it's missing, apply the routine prompt's own inline restatement of the rule instead of stalling on the Read, and flag the drift (this entry) rather than silently re-deriving it every firing unremarked
- Scope: project
- Evidence: 6
- Status: candidate
- observed: 2026-08-23 — recurred a third time picking issue #59: `Glob .claude/skills/work-loop/**` and `find .claude -iname "*work-loop*"` both still came up empty, and the file has now been missing across at least three separate firings (2026-08-20, 2026-08-21, 2026-08-23) with no firing yet opening an issue about the drift itself — falling back to the inline gloss each time works, but the gap in the routine's own config has gone unreported for 3+ days
- observed: 2026-08-23 — recurred a fourth time in a separate firing the same day (picking between ready issues #64/#65): `ls .claude/skills/` and `find .claude -iname "*work-loop*"` again returned nothing; applied the prompt's own inline gloss to judge #64 ready (a normal doc fix with a PR-shaped done-means) and #65 ready-but-PR-less (a stale-branch deletion whose own done-means explicitly waives the PR requirement), picking #64 since it fits the routine's mandatory step-5 PR-delivery flow — the missing file is now confirmed absent across four independent firings spanning three days with still no issue filed about the gap itself
- observed: 2026-08-24 — recurred a fifth time: `ls .claude/skills/` (ten real skills, no `work-loop`) and `find .claude -iname "*work-loop*"` (empty) both re-confirmed on a firing where issue #65 (the only open `task:`-labeled issue) was already known-blocked by tool grant (L-073) from the prior firing's own comment, so the inline gloss was applied to correctly judge arm (b) as having nothing ready at all — not to pick between two candidates this time, but to conclude neither arm had one. Five independent firings across four days, still no issue filed about the missing file itself; this firing also did not file one (focus was step-2c proposals instead) — the gap in the routine's own config remains unreported
- observed: 2026-08-25 — recurred a sixth time (`find .claude/skills -iname "*work-loop*"` still empty) while judging arm (b) readiness; unlike every prior firing, this one finally filed a grounded proposal against the gap itself (issue referencing this entry) instead of only re-applying the inline gloss and moving on — six independent firings across five days was the trigger to stop just working around the drift and report it
- observed: 2026-08-25 — RESOLVED in a later firing the same day: picked up the proposal (issue #73) as arm (b)'s ready task and created `.claude/skills/work-loop/SKILL.md` for real (PR #75), reconciling `docs/ARCHITECTURE.md`'s component-map skill count and `docs/USAGE.md`'s trigger table in the same commit per L-007. The missing-file condition this entry has tracked across six firings should no longer reproduce once #75 merges — a future firing finding `work-loop/SKILL.md` present is this rule's own falsifier; `/evolve` should consider retiring this entry once that's confirmed rather than promoting it, since the underlying drift (not the reflex of checking-then-falling-back) was the actual problem

## L-065 · 2026-08-21 · A hand-maintained id-set that claims "only what was actually invoked" needs its own self-check, or it silently stops meaning that
- Trigger: verifier report on PR #49 (issue #47) — `EXERCISED_CAPABILITY_IDS` in `scripts/build_demo_engagement.py` was hand-written to name exactly the four collectors this build invokes, and the module docstring/comment claimed as much, but nothing derived it from the actual collector calls. The verifier dropped one id from the constant while its collector stayed wired up: only the byte-identity-vs-committed-fixture test failed, none of the "resolves against registry/reconciliation/poisons" tests caught that the `collectors` block no longer matched what was truly invoked
- Rule: when a hand-maintained constant's whole justification is "this matches what the code actually does," add a cheap runtime cross-check against the real thing it claims to mirror (here: each contract's own `.source` field) that raises loudly on mismatch — a comment asserting the match is not a check, and a fixture-drift test only catches it if someone rejects the wrong regenerated fixture instead of recommitting it
- Scope: portable
- Evidence: 1
- Status: candidate

## L-066 · 2026-08-21 · A web/README.md "Q" item without an ANSWERED tag can still be settled — check prior proposal issues' "Not in scope" sections first
- Trigger: this firing scanned `web/README.md`'s open Q-items for a step-2c grounded build-heartbeat proposal after both open PRs were green and every ready plan box/task issue was exhausted. Q17 ("the poison run's verdict records live only inside poisons.json... Should VAL02 emit the poison records into a flat verdicts.json?") carries no `ANSWERED` tag in the README, unlike Q5/Q7/Q8/Q11b/Q12 — but issue #48 (filed by a prior firing) states in its own "Not in scope" section: "Q17's flat-verdicts.json-vs-run-envelope question — that is a live shape fork that C2 deliberately answered with a client-side merge, and it should not be reopened as a side effect of this change." Only reading #48's full body, not just its title or the README, surfaced this; proceeding on the README alone would have re-proposed already-settled work (origin: own observation, caught before filing).
- Rule: before filing a `task:` issue against a `web/README.md` Q-item, grep open issue bodies for that Q-number and read any hit's "Not in scope" section — a Q-item can be deliberately settled by a prior proposal/PR without the README itself being updated to say ANSWERED.
- Scope: project
- Evidence: 2
- Status: candidate
- observed: 2026-08-27 — sharper gap found on Q3 (run-envelope for `verdicts.json`): `search_issues` for "Q3" and grepping the tree for the identifier both returned zero hits, so the Rule as literally written would have cleared Q3 to file with no further check. The actual constraint lived under a *different*, already-`ANSWERED` neighbor: Q17's own README text ends "Took the flat-merge branch of the question, not a run envelope — Q3 stays open," and only reading Q17's full entry (not grepping "Q3") led to issue #48, whose "Not in scope" section names the same flat-vs-envelope fork as deliberately settled. Filed #86 anyway (the README itself still calls Q3 "open"), but rewrote it to lead with this precedent so a future picker doesn't build the envelope Q17 already leaned away from. Sharpening the Rule: grepping only the target Q-item's own number is insufficient when the constraining precedent is attached to a *sibling* Q-item's answer text — before filing, also read every neighboring `ANSWERED` Q-item in the same ledger for a cross-reference back to the target (a "`Q<n>` stays open" or similar phrase), not just the target's own literal number

## L-067 · 2026-08-21 · A "used evidence" artifact keyed by an evaluation identity must be scoped to what was actually evaluated, not everything the pipeline constructed upstream
- Trigger: verify failure (own observation) — building `artifacts/demo-engagement/contracts.json` for issue #48 (emit the EvidenceQualityContracts so /proof's contract stage shows real data), the first draft emitted all four collectors' contracts (hris/okta/github/gcp) keyed by `contract_hash`. `gcp`'s contract is real evidence the pipeline collects, but it is never itself passed as `contract=` to an `evaluate_*` call — gcp's records only feed timelines evaluated under okta's contract — so no verdict record's `spec_hash` ever equals gcp's hash. The join-exactness test (`set(contracts) == {every verdict record's spec_hash}`) failed with gcp's hash as an orphaned extra key; scoping the emitted map to only the three contracts actually passed to an `evaluate_*` call fixed it on the first correction
- Rule: when emitting an artifact meant to be joined by an evaluation-time identity (a contract_hash, a capability id, any key a downstream record cites), derive the emitted set from what was actually passed into the evaluation calls that produced the records — not from every object an earlier pipeline stage happens to construct. A collector/step can produce real evidence that is never itself the operand of a verdict; including it "for completeness" creates a dangling entry a join-exactness test will catch, so write that test before or alongside the artifact rather than after
- Scope: portable
- Evidence: 1
- Status: candidate

## L-068 · 2026-08-21 · Grep for an exact L-NNN id already in the file before assigning it — "highest + 1" can collide across concurrent sessions
- Trigger: own observation during this firing's `/reflect` pass — main's `LEARNINGS.md` already carried two separate entries both headed `## L-062` (one on "a background verifier gates the PR, not the commit", one on "the heartbeat prompt's fallback skill path doesn't exist"), each evidently computed by a different session as "highest existing L-NNN + 1" against a snapshot of main that didn't yet contain the other session's commit; main went on to independently mint `L-064` three times over across separate sessions (the fallback-skill-path entry itself, a Q-item check, and EQC scoping) before this merge caught and consolidated all of it. This is L-058's branch-claiming problem recurring at the granularity of ledger ids instead of plan boxes or branch names
- Rule: before writing a new `## L-NNN` header, grep the checked-out file for that exact id (not just eyeball the last entry) — if the id you were about to use already exists, or a still-open PR's title/body claims it, pick the next id past that claim instead of trusting simple arithmetic. When a merge surfaces an existing collision, don't silently keep two entries sharing one id: renumber the less-established one(s) past the current maximum, preserve every distinct incident's content, and note the renumbering in the surviving entry
- Scope: portable
- Evidence: 3
- Status: candidate
- observed: 2026-08-22 — the rule's own "check still-open PRs" clause was NOT proactively applied on PR #60 (issue #58): `L-070` was assigned by "highest + 1" off a `main` snapshot from before PR #57 opened, and PR #57 (still open) had independently minted its own, different `L-070` entry in the same session window. The collision was only caught by chance — while checking that branch's commit history for an unrelated reason (whether a separate `Reflect:` commit was the established pattern) — not by a deliberate open-PR grep run as part of writing the entry. Renumbered to `L-071` before committing, so no actual collision landed, but the rule's second clause needs a mechanical trigger (e.g. "before assigning L-NNN, list open PRs and diff their LEARNINGS.md against main"), not just prose, or it keeps getting skipped and caught by luck instead
- observed: 2026-08-26 — recurred a third time, and this time the collision was NOT caught before landing on `main`: two concurrent heartbeat firings (issue #78/PR #81, and the Q-item-grep entry from a later same-day firing) each independently minted `## L-078`, and both merged — the second merge (04:55:17 UTC) created a live duplicate header that sat undetected on `main` until this firing's own `/reflect` pass happened to grep for the next id. Confirms the 2026-08-22 note's diagnosis: "highest + 1" without a live open-PR/recent-merge check is still the default behavior three recurrences in, because the check is prose in this ledger entry, not a mechanical step in the `/reflect` skill or a CI grep for duplicate `## L-NNN` headers. Renumbered the second-merged entry to `L-080` per this entry's own rule

## L-069 · 2026-08-21 · ScheduleWakeup is for /loop dynamic-mode sessions only — a background Agent call already auto-notifies on completion
- Trigger: own observation — this firing launched a background adversarial `verifier` agent via the Agent tool (which the tool's own description says will notify automatically on completion) and then also called `ScheduleWakeup` to "wait" for it, only to have the tool's own description turn out to scope it to `/loop` dynamic-mode sessions specifically (its `prompt` field expects the `<<autonomous-loop-dynamic>>` sentinel or a `/loop` invocation) — this session is a scheduled build-heartbeat, not a `/loop` session. Caught before any harm (the stray wakeup was cancelled with `stop: true` in the same turn) but wasted a turn and risked firing a nonsensical dynamic-loop resume on a non-loop session
- Rule: before calling `ScheduleWakeup`, confirm the current session is actually a `/loop` dynamic-mode session — if you are merely waiting on a background `Agent` call's own completion notification, do nothing further; no wakeup scheduling is needed or correct
- observed: 2026-08-23 — RECURRED, worse than the first instance: this firing (issue #59) called `ScheduleWakeup` twice while waiting on the same verifier agent's completion notification, and neither call was cancelled with `stop: true` before the task-notification actually arrived — the second one was left dangling into the next turn and only got cancelled (confirmed via the tool's own "cancelled 1 pending wakeup(s)" response) during the `/reflect` pass that wrote this very note, i.e. after this entry had already been read and its rule already known. A candidate rule with Evidence 1 that the same session type re-breaks worse the very next time it fires is not being applied from memory at all — reading LEARNINGS.md is not the same as consulting it before the action it warns about
- Rule: this entry cannot be trusted to prevent recurrence by presence in the ledger alone; before calling ScheduleWakeup, actually check this rule first — and if a heartbeat firing catches itself mid-mistake, cancel with `stop: true` in that same turn rather than leaving it dangling for a later /reflect to notice
- Scope: portable
- Evidence: 5
- Status: candidate
- observed: 2026-08-25 — recurred yet again (issue #73 firing): called `ScheduleWakeup` to "wait" for a background `verifier` agent's own completion notification and got the identical `InputValidationError: prompt is required when stop is not true` this entry already documents twice over. Did not retry or route around it — read the rejection as confirmation to do nothing further, and the agent's completion notification duly arrived on its own a few turns later. Fourth documented instance of this exact call shape failing the same way; the rule keeps being the correct fix in hindsight but is still not being consulted *before* the call, only recognized after the tool rejects it
- observed: 2026-08-27 — recurred a fifth time (issue #87 firing), triggered under pressure this time: the environment's Stop hook blocked ending the turn (dirty git state) while a background `verifier` agent was still running, and `ScheduleWakeup` was called reactively to try to manufacture a wait — same `InputValidationError` a fifth time, without consulting this entry first. Also tried a standalone `sleep 60` immediately after, which the harness blocks outright ("use Monitor with an until-loop... do not chain shorter sleeps to work around this block"); polled `ListAgents` twice in between. None of it was necessary — the agent's completion notification arrived on its own a few turns later regardless, exactly as this entry's Rule already says. The correct response to a Stop-hook block while a background agent is outstanding is still to do nothing further and let the notification land, not to invent a wait mechanism
- observed: 2026-09-01 — recurred a sixth time (build-heartbeat firing, PR #117/issue #52), and worse in a new way: this call supplied every required field (`delaySeconds`, `prompt`, `reason`, `noop`) correctly, so it did NOT hit the `InputValidationError` the five prior instances relied on to be caught — it silently scheduled a real 276s wakeup with the `<<autonomous-loop-dynamic>>` sentinel on a session that is not a `/loop`. Nothing external flagged this one; it was only caught by re-reading the tool's own description text in the same turn and cancelling with `stop: true` before any harm. The validation-error safety net five prior notes leaned on is not reliable — a well-formed call to the wrong tool succeeds and schedules real (if harmless-in-this-case) state, so the only real defense is checking session type *before* calling, not after

## L-072 · 2026-08-23 · A non-worktree verifier agent's mutation-testing edits share your working tree and can still be present when it reports done
- Trigger: own observation (issue #59, PR #61) — a `verifier` agent was dispatched with no `isolation: 'worktree'` to mutation-test a new ladder test by temporarily editing `LADDER_TRANSITIONS` in `src/aegis_sentinel/schema/models.py`, confirming the test failed, then reverting. Before its completion notification arrived, `git status` (run to satisfy a stop-hook check demanding no uncommitted changes) showed `models.py` as modified with a real one-line diff (the RATIFIED->STALE edge removed) — a mutation still in place mid-agent-run, caught only because `git status` was checked before committing, not because the agent's own eventual "reverted" claim was trusted (consistent with L-005). The agent's final report did confirm a clean revert once it finished, so this was a timing race, not a broken agent — but nothing about dispatching it warned that its intermediate states would be visible, and are safe to observe, in the same checkout the main thread is about to commit from
- Rule: when dispatching an agent whose verification method includes deliberately mutating tracked source files (mutation-testing, revert-based falsifier probes) without `isolation: 'worktree'`, always run `git status`/`git diff` immediately before your own commit, even after the agent reports success — its intermediate edits share your working tree and a commit made mid-mutation would ship broken source. Prefer `isolation: 'worktree'` for such agents when the task can tolerate the setup cost, to remove the race rather than just detect it
- Scope: portable
- Evidence: 2 (independent recurrence, separate session, 2026-08-22)
- Status: candidate
- observed: 2026-08-22 — recurred in a later build-heartbeat firing (issue #54): called `ScheduleWakeup` while waiting on a background adversarial `verifier` agent, this time without even reaching the "then cancel it" step — the call errored outright (`prompt is required when stop is not true`) because a bare wait-notification intent doesn't map onto any of the tool's required fields, not just its documented scope. The tool's hard rejection was itself the signal to stop trying and just wait; no retry, no workaround was attempted
- Renumbered: from `L-070` to `L-072` on 2026-08-23 (issue #62) — this entry and the unrelated `L-070` on "when an issue's done-means literally names N fields..." (PR #57/issue #54, merged first) landed on `main` one minute apart, both minted as `L-070` per L-068's own "highest existing + 1 across concurrent sessions" collision. The other entry keeps `L-070`; this is the one that created the live duplicate, so it moves per L-068's own renumbering rule

## L-070 · 2026-08-22 · When an issue's done-means literally names N fields for a consumer to read directly, verify each named field is actually consumed — not just enough of them to pass the tests
- Trigger: verifier report (adversarial pass, first round) on issue #54/PR #57 — the issue's own done-means said `web/src/data/reconciliation.ts:253-260` and `WhyCompleteRail.tsx:238` should "read `.ref`/`.bucket`/`.dispositioned` directly instead of joining against `buckets`/`dispositions`." The implementation read `.ref` and `.bucket` directly but left the third named field, `.dispositioned`, plumbed through the JSON artifact and the TS type yet never read by any frontend code — `openBlockers` still derived "answered" purely via the `resolveDisposition` join, exactly the pattern the issue asked to stop doing for that field. All backend tests, `tsc --noEmit`, and `next build` passed regardless, because an inert field breaks nothing mechanically; only the verifier's literal re-read of the issue text against the diff caught the gap
- Rule: when a task's done-means lists several fields/properties a consumer should use, after implementing, re-read that exact list against the diff one field at a time and grep each field name in the consuming code — "read three of four literally-named things directly" is not the same as satisfying a done-means that named four, and no test suite will fail on an unread field that happens to be harmless
- Scope: portable
- Evidence: 2
- Status: candidate
- observed: 2026-08-22 — recurred verbatim in a separate build-heartbeat firing (issue #58 / PR #60): launched the `verifier` agent via `Agent`, then called `ScheduleWakeup` anyway to "wait" for it, and got `InputValidationError: prompt is required when stop is not true` back — the tool doesn't even accept a bare wait-and-noop call outside `/loop`. Corrected in the same turn by doing nothing further and letting the agent's own completion notification arrive; independent of the session that first logged this, so this is genuine second evidence, not a re-read of the same incident

## L-071 · 2026-08-22 · An issue's own done-means file list is a floor, not the full blast radius of an artifact-shape change — run the whole verify suite regardless
- Trigger: verify failure — issue #58's own "done means" enumerated exactly which files a fix should touch (the two drift-test modules, `web/README.md`, `verdicts.ts`) and never mentioned `tests/test_mcp_server.py`. After regenerating `verdicts.json` as the consolidated 11-record roster the issue asked for, `./scripts/verify.sh full` (project-wide, 238 tests) turned up two real failures in `test_mcp_server.py::test_query_verdicts_filters`/`test_query_verdicts_via_server_call_tool` — both hardcoded the pre-existing assumption that `verdicts.json` holds exactly one record, a consumer the issue's own scope never named because nobody reasoning from the file list alone would have found it. This was also a wrong prediction on my own part: before implementing, I had reasoned through drift-test/poison-suite/frontend impact but not searched for every non-web, non-listed consumer of the artifact's cardinality
- Rule: treat a task's or issue's enumerated done-means files as a floor, never the full blast radius of a shared-artifact shape/cardinality change — always run the project's full verify suite (not just the tests for the files the issue names) before declaring done, since consumers outside the stated scope can silently assume the old shape and only the whole-suite run will surface them
- Scope: portable
- Evidence: 1
- Status: candidate

## L-073 · 2026-08-23 · An issue's done-means can pre-authorize a destructive git op that the session's own tool grant still refuses at execution time
- Trigger: own observation, build heartbeat firing — issue #65's own done-means named `git push origin --delete <branch>` as the required action for three confirmed-stale branches and explicitly reasoned that "explicit pickup" by a later firing was sufficient authorization (having deliberately not executed it in the firing that proposed it). Re-confirmed all three branches were still safe to delete (empty diff or superseded per L-005's note above), then ran the exact command — it was denied outright by this session's auto-mode tool classifier as a destructive/hard-to-reverse remote operation, with no GitHub MCP tool available as an alternate path (only `create_branch`/`list_branches`, no delete). The issue's own text cannot grant a permission the runtime withholds from an unattended session; this is a sharper, git-specific instance of L-044's general point about auto mode suspending broad guarantees
- Rule: when a task's done-means calls for a destructive/irreversible git or platform operation (branch/tag deletion, force-push, history rewrite) in an unattended/heartbeat session, first read that issue's existing comment thread (not just its body) for a prior denial record before attempting anything; if none exists, attempt it once to confirm, then leave the task open with a comment recording what was verified and that only an interactive or Owner-run session can complete the actual deletion; do not treat repeated denial as something to route around, and do not re-attempt an op already recorded as blocked
- Scope: portable
- Evidence: 2
- Status: candidate
- observed: 2026-08-24 — the rule's "do not treat repeated denial as something to route around" clause held on its first real test: this firing found issue #65 still open with its 2026-08-23 comment already recording the denial, and did not re-attempt `git push origin --delete` at all — treated the prior firing's documented attempt as sufficient and moved on to arm 2c (proposals) instead of wasting a turn re-confirming a known-blocked action
- observed: 2026-08-25 — regressed on the 2026-08-24 refinement: this firing re-verified the three branches' diffs first (good), but then ran `git push origin --delete` again *without* first reading issue #65's existing comment thread, which already recorded the identical denial from 2026-08-23. The command was denied a second time for the same reason, costing a tool call the prior firing had shown was avoidable. Sharpening the Rule: before attempting a destructive op an open issue proposes, read that issue's comments first, not just its body — the denial record lives in a comment, not the issue text
- observed: 2026-08-25 (second firing) — held on the sharpened refinement: found issue #65 open with both prior denial comments already on the thread, read them before doing anything, and did not re-attempt `git push origin --delete` at all — treated the two recorded denials as sufficient and moved straight to arm 2c
- observed: 2026-08-26 — regressed again, same shape as the 2026-08-25 first-firing lapse: re-verified the three branches' diffs (good), but ran `git push origin --delete` directly without first reading issue #65's comment thread, which already carried two prior denial comments (2026-08-23, 2026-08-25) recording the identical block. Denied a third time for the same reason. The Rule as written ("read that issue's existing comment thread... before attempting anything") is being read inconsistently across firings — worth promoting via `/evolve` (Evidence already ≥2) so this becomes a checked step rather than something each firing re-derives from the ledger text
- observed: 2026-08-27 — held again: read issue #65's comment thread (now three prior denial comments: 2026-08-23, 2026-08-25 ×2, 2026-08-26) before doing anything, re-verified the three branches' diffs were still empty/superseded, and did not re-attempt `git push origin --delete` at all. Went further than the Rule requires and also skipped posting a fourth near-identical "re-confirmed, still blocked" comment — see new entry L-081 for that separate judgment call

## L-074 · 2026-08-24 · When both build-plan and issue arms are empty, actively scan for uncovered gaps before filing "nothing to propose"
- Trigger: own observation, build heartbeat firing — with every EXECUTION-PLAN.md box ticked or D7-blocked, the sole open `task:` issue (#65) already confirmed blocked by tool grant (L-073), and the remaining open issues either Owner-only (`question:` #46/#51/#52, Owner-settings #20) or out of a sandboxed session's reach (#42 references personal transcripts never provided; #10 references an unfetched zip; #4 is explicitly "captured work, not a work order" from a different dispatch process in another repo), a first pass looked like a genuine step-2c "nothing ready, propose and stop" firing. Rather than stopping there, grepped `web/README.md` for Q-items missing the `ANSWERED` tag (Q9, Q10, Q13 among them) and ran `search_issues` for each one's exact wording before assuming any was already covered — two (Q9, Q10) had never been proposed and became grounded issues #68/#69; a third (Q13) was checked and correctly left unfiled because, unlike Q9, no in-scope field exists to expose (`SourceRef` at `models.py:47-50` carries no time-window concept at all — genuine new modeling, not exposure, so filing it would repeat the Q11b/#59 overclaim this ledger already warns against)
- Rule: before a step-2c firing concludes "nothing to propose," do not stop at confirming the obvious sources (EXECUTION-PLAN's Owner-open-items list, currently-open issues) are all already tracked — grep the project's own open-question ledgers (here, `web/README.md`'s Q-items) for entries missing their resolution tag, and exact-phrase `search_issues` each candidate before either filing it as new or discarding it as already-covered; L-066's existing rule governs the verification half of this (check a candidate's own history before refiling), this entry governs the discovery half (find the candidate in the first place instead of concluding none exist)
- Scope: project — the specific ledger (`web/README.md`'s Q-items) is this repo's; the general "enumerate, don't assume-covered" principle is likely portable but unproven outside this repo's Q-item convention
- Evidence: 2
- Status: candidate
- observed: 2026-08-25 (second firing) — fired again with the same shape: EXECUTION-PLAN.md fully ticked/D7-blocked, issue #65 confirmed blocked (L-073), issue #73 already claimed by open PR #75. Scanned `web/README.md`'s Q-items and `search_issues`/the closed-issues list per the Rule; two genuinely uncovered candidates (Q4b, Q6) became grounded issues #77/#78. Naive `grep -v ANSWERED` over Q-item lines produced false positives (Q5/Q7/Q8/Q12 flagged unanswered when they carry the tag on a later line of the same multi-line bullet) — caught only by reading the full bullets, not by the Rule itself; see new entry L-076 for the sharpened technique
- observed: 2026-08-28 — fired a further time: with #86/#87/#94 all already claimed by open PRs and #65/#93 both correctly excluded (tool-grant-block and unmerged-dependency-block respectively — see new entry L-082 for the latter), read every full Q-item bullet in `web/README.md` end to end rather than grepping, and `search_issues`'d each un-ANSWERED-or-partially-open candidate found (Q16 fully open; Q11/Q11b's "still open" sub-notes). Both were genuinely uncovered (all prior searches empty) and became issues #97/#98

## L-075 · 2026-08-25 · This repo's PostToolUse Edit hook lints per-file after every Edit call — split an import from its first usage across two Edit calls and the hook fires red on the first
- Trigger: verify failure (own observation), three times in one session (issue #69) — adding `D7Family` to `schema/__init__.py`'s import block in one `Edit` call before a second `Edit` added it to `__all__` tripped `ruff`'s F401 "imported but unused" via `.claude/hooks/post-edit-verify.sh` (`scripts/verify.sh quick`); the identical shape recurred adding `computed_field` to `pydantic`'s import in `schema/models.py` before the edit that used it, and again adding `D7Family` to a test file's import block before the test using it. All three were harmless and self-resolved by the very next `Edit` call, but each cost a full blocked-tool-call round-trip
- Rule: when a change adds both a new import and that import's first real usage, and both land in the same file, make them in a single `Edit` call (or order multi-call edits so usage lands before/with the import, never strictly after) — this repo's quick-verify hook lints the file after every individual `Edit`, so a logically-atomic import+usage change split across calls transiently fails even though the finished diff is correct
- Scope: project — depends on this repo's specific PostToolUse `ruff`-on-save hook; the general principle (batch an import with its first usage when a file-level lint-on-save hook exists) is likely portable but unproven elsewhere
- Evidence: 4
- Status: candidate
- observed: 2026-08-25 (separate session, issue #77/PR #80) — recurred in a broader shape than the Rule's literal "import+usage" wording: adding `from aegis_sentinel.schema import AssertionType` alone fired the same F401 (exact match); then removing the first of two duplicate `GROUP_TO_ASSERTION_TYPE` dicts before both of its call sites (`tests/test_poison_suite.py:267` and `:429`) were updated to match fired F821 "undefined name" twice, one call site at a time, across three separate `Edit` calls — same root cause (the hook lints per individual `Edit`, so a logically-atomic multi-site change split across calls transiently fails) but manifesting as a stale reference rather than an unused import. The Rule as written only names the import+usage case; this generalizes it to any identifier whose definition and every one of its usages don't land in the same `Edit` call
- observed: 2026-08-27 — recurred again (issue #87 firing), the exact literal import+usage shape: added `VerdictState` to `scripts/build_demo_engagement.py`'s `from aegis_sentinel.schema import (...)` block in one `Edit` call before a second `Edit` call gave it its first usage in the new `_classification`/`_record_class` helpers — tripped the identical F401. Self-resolved by the very next `Edit` as always; still costs one blocked round-trip every time this ordering is chosen over combining the two changes into one `Edit` call
- observed: 2026-08-28 (issue #97/PR #101) — recurred three times in one session: `CommitmentSource`/`Commitment` added to two files' import blocks before the edit using them (F401, twice), then `commitments_artifact` assigned before the edit that added it to the returned artifacts dict (F841 unused variable) — the same split-definition-from-usage shape as the 2026-08-25 F821 case, now also covering an unused local variable, not just imports

## L-076 · 2026-08-25 · A single-line grep for a ledger's resolution tag false-positives on multi-line entries — read the whole entry before concluding it's unresolved
- Trigger: own observation, build heartbeat firing applying L-074's Q-item scan — `grep -n "Q[0-9]" web/README.md | grep -v "ANSWERED"` flagged Q5, Q7, Q8, and Q12 as unanswered, because each of those Q-items' `*(ANSWERED at ...)*` tag sits on a line below the `**Q<n> — ...**` heading line inside the same multi-line bullet, not on the heading line itself. `Read`-ing the full bullets (not grepping line-by-line) showed all four were already settled; only Q4b and Q6 were genuinely open and became issues #77/#78
- Rule: when scanning a ledger of multi-line bulleted entries for ones missing a resolution tag, do not grep tag-presence per matched line — grep for the entry markers first (e.g. `^- \*\*Q[0-9]`), then read each full entry (to the next marker) before deciding it lacks the tag; a per-line `grep -v <tag>` only catches entries where the tag happens to share a line with the heading
- Scope: portable — the multi-line-bullet-vs-per-line-grep mismatch applies to any ledger scanned this way, not just this repo's `web/README.md`
- Evidence: 1
- Status: candidate

## L-077 · 2026-08-25 · Create the task branch before making any edits, not after — a build session can silently drift onto main
- Trigger: own observation, build heartbeat firing (issue #77/PR #80) — the heartbeat prompt's own step order names BRANCH (`git checkout -B claude/<task-id>-<slug> origin/main`) as step 1, strictly before PICK THE WORK (step 2) and BUILD (step 3). This firing ran the claim-check, picked issue #77, and made all seven files' edits directly on the local `main` checkout without ever running the branch-creation command — caught only because the session-end stop hook's uncommitted-changes check prompted a `git status`/`git branch --show-current`, which showed `main` still checked out with the finished diff sitting on it, uncommitted. Recovered cleanly (`git checkout -B <branch> origin/main` carries uncommitted changes onto the new branch; `main` was never committed to), but nothing in the edit/verify loop itself would have caught the drift on its own — quick-verify hooks lint per-file, not per-branch, and `scripts/verify.sh full` doesn't check which branch it's running on
- Rule: run the branch-creation command as the very first action after the claim-check, before touching any file — never defer it until "after I've figured out what to build," since by then edits already exist on whatever branch happened to be checked out. If a mid-task check ever finds edits sitting on `main` with nothing committed yet, `git checkout -B <branch> origin/main` is the safe recovery (it carries the uncommitted worktree state to the new branch and leaves `main` untouched)
- Scope: portable
- Evidence: 1

## L-080 · 2026-08-26 · When investigating an open Q-item's status, grep the codebase for the Q-item's own identifier, not just re-read the README bullet
- Trigger: own observation, build heartbeat firing — the arm 2c gap-scan (L-074) flagged `web/README.md` Q1/Q2 as candidates: no `ANSWERED` tag, no existing GitHub issue (confirmed via `search_issues` per L-066). Investigating a done-means for them led first to `web/src/data/types.ts:139`'s inline comment ("see README questions Q1/Q2 on the naming drift vs. schema/models.py Verdict"), which pointed to `web/src/data/bridge.ts:12-19`'s "DELIBERATELY ABSENT" block — a full, reasoned answer to both questions, already implemented and documented when C1 landed, just never written back into the README's own Q-item ledger as `ANSWERED`. Re-reading only the README bullet (the L-074/L-076 technique) would have missed this entirely and risked either filing a needless `question:` issue asking the Owner to re-decide something already decided, or a `task:` issue proposing to unilaterally "fix" a divergence that a prior session deliberately chose to keep, for documented reasons.
- Rule: before filing an open Q-item as a `question:` issue (needs Owner ruling) or a from-scratch `task:` issue (needs new work), grep the whole repo for the Q-item's own identifier (`Q1`, `Q4b`, `Q13`, etc.) — not just the README bullet that raised it — since a later task may have already resolved it and left the rationale in a code comment citing the question by name, without circling back to mark the ledger entry `ANSWERED`. If found, the resulting task is a documentation-only ledger update citing the existing rationale, not new design work or an Owner ask.
- Scope: project — the specific ledger (`web/README.md` Q-items) and its in-code citation convention (comments naming "Q1/Q2" etc.) are this repo's; the general technique (grep an open item's own identifier across the whole tree before assuming it's unresolved) is likely portable but unproven elsewhere.
- Evidence: 1
- Status: candidate
- Renumbered: from `L-078` to `L-080` on 2026-08-26 (this firing's `/reflect`, issue #82) — this entry and the unrelated `L-078` on `build_demo_engagement.py`'s two silent entry points (issue #78/PR #81, merged first at 01:03:47 UTC) both landed on `main` headed `## L-078`, minted independently by concurrent heartbeat firings per L-068's own failure mode. The `build_demo_engagement.py` entry merged first and keeps `L-078`; this is the one whose merge created the live duplicate, so it moves, past the current max (`L-079`) per L-068/L-072's precedent. (Also fixed a duplicated `- Status: candidate` line this entry had carried since it was first written.)

## L-078 · 2026-08-26 · `scripts/build_demo_engagement.py` has two silent CLI-arg-selected entry points — running it without `poisons` regenerates the wrong, smaller artifact set with no error
- Trigger: own observation (wasted path), build heartbeat firing (issue #78/PR #81) — needed to regenerate `artifacts/demo-engagement/*.json` after a `BoundaryExclusion` wire-shape change. Ran `python scripts/build_demo_engagement.py` (no argument); it exited 0 and printed a single `wrote .../verdicts.json` line, and `git diff --stat` looked plausible at a glance. Only inspecting the diff in full showed `verdicts.json` had shrunk from ~293 lines to a 1-record stub — the script's `if __name__ == "__main__": sys.exit(main_poisons() if sys.argv[1:] == ["poisons"] else main())` silently routes to `main()`, a different, smaller single-population "walking skeleton" demo, unless the literal string `poisons` is passed; only `main_poisons()` regenerates the full five-artifact poison-suite set (`contracts.json`, `poisons.json`, `reconciliation.json`, `registry.json`, `snapshot.json`, `verdicts.json`) the repo actually ships and tests against. Recovered with `git checkout -- artifacts/demo-engagement/verdicts.json` followed by `python scripts/build_demo_engagement.py poisons`, which correctly regenerated all five files and printed the `100%` detection-rate line
- Rule: when regenerating `artifacts/demo-engagement/`, always run `python scripts/build_demo_engagement.py poisons` (never bare) — the bare form is a different, smaller demo variant that succeeds silently and can overwrite `verdicts.json` with stale/wrong content. After running either form, `git diff --stat` on `artifacts/demo-engagement/` and confirm every file you expected to change actually shows a diff (and none you didn't expect shrank unexpectedly) before trusting the regeneration
- Scope: project
- Evidence: 1
- Status: candidate

## L-079 · 2026-08-26 · A `correction:` line in `.session-signals` is not proof of an actual user correction — it can be a keyword false-positive on non-user text
- Trigger: own observation — `.claude/hooks/prompt-signal.sh` is a `UserPromptSubmit` hook that regex-matches the submitted `.prompt` field against a correction-keyword pattern (including bare `wrong`) and logs a `correction:` signal on any match, with no check that the submitted text is a live human message. This session, an async `verifier` agent's completion `<task-notification>` was delivered as a prompt-submit event; its report text included the phrase "Green could still be wrong if..." (an adversarial counterfactual, not a correction), and the hook logged `correction:...:wrong:<task-notification> <task-id>...` exactly as if the user had typed a correction. No live user input occurred at any point in this session (it is a scheduled heartbeat firing) — the entire signal was manufactured from a subagent's own analytical prose
- Rule: before treating a `correction:` entry in `.session-signals` (or a Stop-hook "leftover signals" nudge derived from it) as evidence a real user correction occurred, read its logged excerpt and confirm the matched text actually came from a human message in this session — task-notifications, tool results, and other non-user text delivered through the same prompt-submit channel can trip the same keyword regex and must not be written up as a lesson about user feedback
- Scope: project — depends on this repo's specific `prompt-signal.sh` keyword-matching implementation and on the harness delivering task-notifications through the same channel it uses for user prompts; the general principle (a keyword-based signal detector can't distinguish its intended source from other text arriving on the same channel) is likely portable but unproven elsewhere
- Evidence: 2
- Status: candidate
- observed: 2026-08-27 — recurred verbatim in a separate heartbeat firing (issue #94): a background `verifier` agent's completion `<task-notification>` carried the phrase "could still hide a wrong period-vs-window comparison" in its own adversarial counterfactual reasoning, tripping the same bare-`wrong` regex and logging `correction:...:wrong:<task-notification>...` in `.session-signals` — again with zero live user input anywhere in the session (a scheduled build-heartbeat firing). Confirmed false-positive the same way: read the logged excerpt, saw it was subagent prose delivered through the prompt-submit channel, not a human message

## L-081 · 2026-08-27 · Don't post another status comment on a blocked task once its thread already carries multiple identical re-confirmations
- Trigger: own observation, build heartbeat firing — issue #65 (delete three stale/superseded branches, blocked by this environment's destructive-op tool-grant per L-073) already carried three consecutive near-identical comments (2026-08-23, 2026-08-25, 2026-08-26), each re-verifying the same branches and re-stating the same denial. The work-loop skill's step 5 instructs leaving "a short comment noting the re-check" whenever a chosen issue's blocker is re-verified unchanged. This firing re-verified the branches were still stale/superseded (no push attempted, per L-073) but judged a fourth comment repeating the identical content would be pure noise carrying no new information, and skipped it — noting the re-check only in this session's own final summary instead
- Rule: when a task's comment thread already carries two or more consecutive re-confirmation comments recording the identical unchanged blocker, do not add another purely to satisfy a "note the re-check" instruction — the general "be frugal about posting" guidance overrides a skill's literal per-firing comment step once the marginal comment adds no information. Record the re-check in the firing's own summary/reflect notes instead, and only comment on the issue again once something has actually changed (new branch content, a tool-grant change, the Owner acting on it, etc.)
- Scope: project — the specific issue (#65) and skill (work-loop) are this repo's, but the general principle (stop posting identical status comments once the record already exists) likely generalizes to any repeated-firing task-tracking pattern
- Evidence: 2
- Status: candidate
- observed: 2026-08-28 (issue #97/PR #101 firing) — fired again: issue #65's thread already carried four near-identical re-confirmation comments (through 2026-08-27); this firing re-checked the blocker via the issue's existing comments rather than re-verifying the branches itself (out of scope this firing — a different task was picked), and did not add a fifth comment, consistent with this entry's rule

## L-082 · 2026-08-28 · A follow-up task issue whose done-means assumes a sibling PR already merged needs the PR's actual merge state checked, not just its existence
- Trigger: own observation, build heartbeat firing — issue #93 ("Q4 follow-up — structure PoisonEvidence per classification kind") opens by asserting "Q4 (now ANSWERED by PR #91 / issue #87 ...)" and its done-means is written entirely in terms of extending the `PoisonClassification`/`_classification()` structure PR #91 introduces. At this firing, PR #91 was still open (not merged) — confirmed directly against `origin/main`'s actual file content (`web/src/data/types.ts` still typed `actual_class: string`; `build_demo_engagement.py` still emitted bare strings), not by trusting #93's own narrative or the fact that #87/#91 existed. Picking #93 as-is would have required either re-implementing PR #91's unmerged structure inside the same PR (duplicating in-flight work) or branching off PR #91's head instead of `origin/main` (against the heartbeat's own step-1 branching rule) — both are exactly the collision shape step 0's claim-check exists to prevent, just one hop removed: the collision isn't with #93 itself, it's with the *prerequisite* #93 silently assumes is already in
- Rule: before picking a task issue whose done-means references or builds on another issue/PR's specific code changes ("now that #N added X, do Y to X"), verify that referenced PR has actually merged into the branch point you're about to build from (check `origin/main`'s real file content, not the issue text or the PR's existence/open-state) — an unmerged prerequisite makes the dependent issue not-ready this firing, the same as an open `question:` dependency or a `loop:hold` label, even though nothing marks it as blocked
- Scope: project — the specific `web/README.md` Q-item chain and heartbeat step-0 claim-check are this repo's; the general principle (a task issue's stated prerequisite can itself still be in flight — verify against the actual base branch, not the issue's narrative) is likely portable to any multi-firing/multi-agent issue queue where follow-up issues get filed against work still mid-review
- Evidence: 1
- Status: candidate

## L-083 · 2026-08-28 · `source venv/bin/activate` does not reliably update `PATH` for a subsequent command in this harness's Bash tool — invoke the venv's binaries by explicit path instead
- Trigger: own observation, build heartbeat firing (issue #97/PR #101) — after creating and successfully using a Python 3.12 venv via its binaries directly (`.venv/bin/python`, `.venv/bin/pip`), ran `source .venv/bin/activate && which python && python --version` as a single chained Bash-tool command; `which python` resolved to `/usr/local/bin/python` (the ambient 3.11 system interpreter), not the venv's, and `python --version` confirmed 3.11.15. Re-tried with the venv's absolute path in `source` and with a fresh `hash -r`-equivalent re-check; the activation still did not take effect within that Bash tool call, even though `&&`-chaining should keep one shell's state for the whole command. Invoking `.venv/bin/python`/`.venv/bin/pytest` directly by path worked correctly every time, including in the same commands where `source .../activate` had just failed to redirect `which python`
- Rule: in this harness's Bash tool, do not trust `source <venv>/bin/activate` to make subsequent bare commands (`python`, `pytest`, `pip`, `ruff`, ...) resolve to the venv's binaries, even within one chained (`&&`) command — invoke the venv's binaries by explicit relative or absolute path (`.venv/bin/python`, `.venv/bin/pytest`) instead, or prefix `PATH="$PWD/.venv/bin:$PATH"` on the same command line rather than relying on `source activate`'s PATH mutation
- Scope: project — observed only in this repo's remote container/session; whether this is a property of the Claude Code Bash tool generally (portable) or specific to this container's shell setup is unconfirmed, so kept at project scope until independently observed elsewhere
- Evidence: 1
- Status: candidate
- observed: 2026-09-01 (build heartbeat, issue #52/PR #117) — contradicting data point: `source <venv>/bin/activate && python scripts/build_demo_engagement.py`, `... && ./scripts/verify.sh full`, and similar chained commands correctly resolved to the venv's Python 3.12 every time in this same container/session — no fallback to the ambient interpreter was observed. Did not retest the specific `which python` probe this entry's Trigger used, so this doesn't directly refute it, but it's inconsistent with "does not reliably update PATH" as a blanket claim for this harness. Left as an observed note rather than a rewrite — worth an actual `which python` re-check next time this fires before treating the rule as settled

## L-084 · 2026-09-01 · A venv created inside the repo tree can fail `verify.sh`'s repo-wide content scanners on vendored third-party package files
- Trigger: own observation, build heartbeat firing (issue #52/PR #117) — created a Python 3.12 venv at `/home/user/aegis-sentinel/.venv-heartbeat` (inside the repo working tree) to run `pip install -e '.[dev]'` and `./scripts/verify.sh full`. The redaction gate step failed with "found forbidden pattern (bHlvbg==)" and "found forbidden first name", both false positives inside `.venv-heartbeat/lib/python3.12/site-packages/` — pip's vendored `pyparsing`/`colorama` source and the `pygments`/`pip` dist-info `AUTHORS` files happened to contain byte sequences and a first name matching the gate's forbidden-pattern list. The gate walks the whole tree rather than respecting `.gitignore`, so an uncommitted, gitignored venv directory is still scanned. Deleted the venv and recreated it outside the repo (this session's scratchpad directory); the same `verify.sh full` run then passed cleanly on an otherwise-identical diff
- Rule: in this repo, always create any Python venv (or other dependency-installing directory containing vendored third-party source) outside the repo's working tree — e.g. in the scratchpad/`/tmp` — never inside it, even though it would be gitignored; `scripts/verify.sh`'s redaction gate and any similar whole-tree content scanner will scan it regardless of git-ignore status and can false-positive on vendored packages' own license/author/source files
- Scope: project — this repo's specific redaction gate is what makes the failure concrete; the general principle (an in-tree venv is visible to whole-tree scanners regardless of `.gitignore`) is likely portable to any repo running a similar secret-scanning or content-gate script, but unconfirmed elsewhere
- Evidence: 1
- Status: candidate
