{
  "_seeded_drift": "The 'verify' required status check has been silently removed from contexts — everything else matches capture_pass.json. The control MUST turn red on this capture; a green run against it means the collector cannot detect the drift it exists to catch.",
  "repository": "Vinylfigure/aegis-sentinel",
  "branch": "main",
  "collected_at": "2026-08-16T00:00:00Z",
  "status_code": 200,
  "protection": {
    "required_status_checks": {
      "strict": true,
      "contexts": []
    },
    "required_pull_request_reviews": {
      "required_approving_review_count": 1,
      "dismiss_stale_reviews": true
    },
    "enforce_admins": { "enabled": true },
    "allow_force_pushes": { "enabled": false },
    "allow_deletions": { "enabled": false }
  }
}
