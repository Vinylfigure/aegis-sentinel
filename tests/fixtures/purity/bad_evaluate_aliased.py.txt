# Seeded-bad fixture: aliasing must not dodge the AST resolver, and
# os.environ / uuid are banned in strict lanes (D-P3). Never importable.
import uuid
from os import environ
from time import time as wall_clock


def stamp() -> str:
    return f"{uuid.uuid4()}-{wall_clock()}-{environ['HOME']}"
